# AGENTS.md

**Status freshness check:** 2026-06-27 — **Engine 2 — Multi-Box Discovery Scoring, V1: 9 boxes (2026-06-27):** Extended fundamental discovery from a single 6-signal inflection composite into a **9-box peer-relative model** (calc version `discovery_9_box_v1.0`): Box 1 Inflection (the former composite), + Earnings/Revenue Surprise, Residual Price Momentum, Cash Conversion & Accrual Quality, Inflection Persistence, Balance-Sheet Strength, Valuation, Forecast Confidence, Dilution & Capital-Raising. Each box = mean of its **peer z-scored** components; **Composite = equal-weight average of available box scores, requires ≥ 8 valid boxes** (else `INSUFFICIENT DATA`, excluded from rank). Two-level standardization reuses the existing `winsorize`/`zScores`/`rankAndDecile`/`resolvePeerGroups` primitives. **Data layer:** added nullable [FundamentalPeriod](prisma/schema.prisma) columns (interest expense, SBC, ΔWC, common stock issued/repurchased, dividend yield, FCF yield, interest coverage) + new `EarningsSurprise` model (per-report EPS/revenue actual-vs-consensus from FMP `/stable/earnings`, write-once, `@@unique([ticker, reportDate])`); extended FMP provider ([types.ts](src/infrastructure/providers/fmp/types.ts), [statements.ts](src/infrastructure/providers/fmp/statements.ts), [ratios.ts](src/infrastructure/providers/fmp/ratios.ts), [earnings-calendar.ts](src/infrastructure/providers/fmp/earnings-calendar.ts)); earnings ingestion ([fundamental-earnings.service.ts](src/server/services/fundamental/fundamental-earnings.service.ts)) wired into the weekly job; one-shot enrichment [scripts/fundamental-enrich-boxes.ts](scripts/fundamental-enrich-boxes.ts) (`job:fundamental-enrich-boxes`, `--earnings-only` fast path) backfilled 11,853 surprises (0 failures). **Pure math libs** ([src/lib/fundamental/](src/lib/fundamental/)): `surprise.ts`, `residual-momentum.ts`, `cash-quality.ts`, `persistence.ts`, `balance-sheet.ts`, `valuation-box.ts`, `forecast-confidence.ts`, `dilution.ts`, `boxes.ts` (registry = single source of truth), two-level `box-scoring.ts`, `box-inputs.ts`, `flags.ts` (14 trap/data-quality flags, **display-only — never alter the V1 composite**), `box-correlation.ts` (cross-box duplication review). **Orchestrator** [fundamental-box-scoring.service.ts](src/server/services/fundamental/fundamental-box-scoring.service.ts) replaces the deleted `fundamental-scoring.service.ts` (all 3 call sites updated): bulk-loads periods/snapshots/refs/estimates(+~90d prior)/earnings/prices, assembles components, runs the two-level scorer (residual momentum vs the equal-weight subsector benchmark — the one cross-ticker signal), writes `FundamentalScore` (`scoreMethodologyVersion` + audited `scoreJson.boxes[]` carrying raw + z per component) + `FundamentalSectorAggregate` + `DiscoveryQueueSnapshot`. **UI:** [DiscoveryRankTable.tsx](src/components/analysis/fundamentals/DiscoveryRankTable.tsx) generalized from 6 hard-coded inflection sub-columns to a `BOX_COLS` registry (Composite/Decile + 9 box columns + Val) with flag chips; [DiligencePanel.tsx](src/components/analysis/fundamentals/DiligencePanel.tsx) gained an auditable per-box breakdown (box score + each component's raw/z); [DiscoverySummary.tsx](src/components/analysis/fundamentals/DiscoverySummary.tsx) + [discovery-summary.ts](src/lib/fundamental/discovery-summary.ts) roll up the 9 box scores; client [types.ts](src/components/analysis/fundamentals/types.ts) gained `FundamentalScoreJson`/`BoxAudit`. **Auditability:** every score reconstructs from `scoreJson` (components, raw, peer z, box scores, flags, version); deterministic + idempotent. **Live-validated** on the full universe: 2,558 names scored, 2,530 with a composite (2,090 with all 9 boxes), 16 sectors / 76 subsectors; cross-box correlation (`job:fundamental-box-correlation`) shows all pairs |rho| < 0.40 — no duplication at the 0.80 flag threshold. Boxes 8/9/11 deferred; financials/REITs flagged only; BACKFILL data is restated-basis (backtests inherit look-ahead bias; true point-in-time accrues forward via weekly LIVE snapshots). **`npm test` 726** (was 660; +66) **· `npx tsc --noEmit` clean · `prisma db push` clean.** **FA view — per-column EV/NTM valuation + TTM net-leverage fix (2026-06-27):** Fixed two FA Valuation Metrics issues. (1) **Net Leverage** was busted on the quarterly view (HPK Q1 26A `−148.5x`) because per-quarter columns rendered FMP's stored `netDebtToEbitda` (net debt ÷ a *single quarter's* EBITDA, FMP's unreliable basis) while LTM used net debt ÷ **TTM** EBITDA — a mixed basis. Now computed the **same way in every column** (net debt ÷ TTM EBITDA via `netLeverageAsOf`, [fundamental-financials.service.ts](src/server/services/fundamental/fundamental-financials.service.ts)) so it reconciles with the EBITDA row (HPK now 0.86→1.92x). (2) **Historical columns now carry valuation multiples**: instead of only the LTM column, the multiples are `EV / NTM` at **every** column where NTM = **sum of the next 4 quarters** forward from that column, from a unified `actual + forward-consensus` timeline (`buildQuarterTimeline`/`ntmAfter`). Historical columns priced at the period's own stored EV/mktcap over the next 4 actual quarters; LTM + forward columns priced at the current snapshot EV/mktcap over consensus NTM. Builder reworked ([financials.ts](src/lib/fundamental/financials.ts)): `FaForwardValuation`+`ltmIndex` replaced by per-column `FaColumnValuation`; `buildMetricRows(returns, valuations)` maps both arrays 1:1 to columns. **Tests** [tests/fundamental/financials.test.ts](tests/fundamental/financials.test.ts) (23). **`npm test` 660 · `npx tsc --noEmit` clean.** Live-verified on HPK. **FA view — Valuation rename + Valuation Metrics & Return/Profitability sections (2026-06-27):** Renamed the FA bridge header `Capitalization → Enterprise Value` to **Valuation** and added two sections below it ([FinancialsTable.tsx](src/components/analysis/fundamentals/FinancialsTable.tsx)). **Valuation Metrics**: forward `TEV / NTM Revenue`, `TEV / NTM EBITDA`, `NTM P/E` (anchored only to the **LTM** column since NTM is a current consensus) + `Net Leverage (Net Debt / EBITDA)` per historical column + LTM. **Return and Profitability Metrics**: ROE (NI/equity), ROA (NI/assets), ROIC (stored FMP) per column where data exists (estimate columns blank). **NTM** = rolling sum of the next 4 forward quarterly consensus periods (fallback: next forward annual) from Engine 1's `RevisionSnapshot.estimatesJson`; NTM P/E = current market cap / NTM net income; Net Leverage uses the full-TTM stored ratio (not the /4 run-rate); the LTM ROE/ROA use **un-divided** full-TTM net income. Pure builder ([financials.ts](src/lib/fundamental/financials.ts)): `FaPeriodInput` gained `totalEquity`/`totalAssets`/`roic`/`netDebtToEbitda` (carried through `aggregateAnnual`) + new `FaMetricRow`/`FaColumnMetrics`/`FaForwardValuation` types + pure `buildMetricRows`. New `formatMultiple` (1 dp + `x`) in [format-statement.ts](src/lib/fundamental/format-statement.ts). Service ([fundamental-financials.service.ts](src/server/services/fundamental/fundamental-financials.service.ts)) selects the 4 new period fields + snapshot `roic`/`netDebtToEbitda`, builds a column-aligned `FaColumnMetrics[]`, computes NTM + current TEV, and adds `valuationMetrics`/`returnMetrics` to the payload. **New tests** [tests/fundamental/financials.test.ts](tests/fundamental/financials.test.ts) (22: + `buildMetricRows` math, forward-multiples-only-at-LTM, per-column net leverage, null estimate columns, `formatMultiple`). **`npm test` 659** (was 654) **· `npx tsc --noEmit` clean.** Display-only over stored values. **FA view — quarterly default, LTM run-rate, 2yr consensus, A/E labels (2026-06-27):** Enhanced the Financials sub-tab: (1) defaults to **Quarterly** ([FinancialsTable.tsx](src/components/analysis/fundamentals/FinancialsTable.tsx)); (2) the trailing column is renamed **LTM** and on quarterly basis shown as an **average-quarter run-rate** — income-statement flows (Revenue→FCF) / 4 while point-in-time cap items (Market Cap/Cash/Debt/Preferred/EV) + shares stay intact (cap→EV bridge correct, per-share auto-scales), via pure `toQuarterlyRunRate` ([financials.ts](src/lib/fundamental/financials.ts)); (3) forward consensus now shows **two years** — up to 2 annual / 8 quarterly forward columns from Engine 1 `RevisionSnapshot.estimatesJson` (revenue/EBITDA/net income/EPS only; others blank), each with its analyst count (`numAnalystsRevenue ?? numAnalystsEps`) as an `n=NN` sub-line under a spanning accent-tinted **CONSENSUS ESTIMATES** group header; (4) Bloomberg **A/E labels** — historical `Q2 24A`/`FY24A`, estimate `Q4 26E`/`FY26E` via pure `buildMonthMap` + `actualLabel`/`estimateLabel` (off-calendar fiscal years map estimate dates onto the right fiscal quarter/year). Service [fundamental-financials.service.ts](src/server/services/fundamental/fundamental-financials.service.ts) threads labeler + run-rate + multi-estimate builder; `FaColumnInput.estimateAnalysts` + `FaColumn.analysts` added. **Tests** [tests/fundamental/financials.test.ts](tests/fundamental/financials.test.ts) (17). **`npm test` 654** (was 630) **· `npx tsc --noEmit` clean.** Live-verified MU both bases. **Known follow-up (separate, data not display):** recent MU quarterly actuals + estimate magnitudes in the FMP-sourced `FundamentalPeriod`/`estimatesJson` look inflated/cumulative — a data-accuracy investigation; this change renders stored values as-is. **Engine 2 — Financial Analysis (FA) view (2026-06-27):** New Bloomberg-FA-style **Financials** sub-tab in the Fundamentals module ([FinancialsTable.tsx](src/components/analysis/fundamentals/FinancialsTable.tsx), wired in [FundamentalsClient.tsx](src/components/analysis/fundamentals/FundamentalsClient.tsx), shares the ticker with Diligence): per-stock historical income block (Revenue → Gross Profit → EBITDA → Net Income → CFFO → CAPEX → FCF) with a margin (Revenue: YoY growth) sub-row under each line, a separate per-share build, and a Market Cap → Enterprise Value (TEV) bridge; **Annual / Quarterly** toggle + units selector. **Annual derived** from the stored quarters (flows summed over 4 fiscal quarters, balance/market-cap at fiscal-year-end — no annual re-ingest). Forward **estimate column** from Engine 1 `RevisionSnapshot.estimatesJson` (Revenue/EBITDA/NetIncome/EPS only). **Preferred & Other** in the TEV bridge required capturing balance-sheet preferred equity + minority interest: extended FMP mapping ([types.ts](src/infrastructure/providers/fmp/types.ts), [statements.ts](src/infrastructure/providers/fmp/statements.ts)) + two additive nullable `FundamentalPeriod` columns ([schema.prisma](prisma/schema.prisma), `db push` clean), threaded through ingestion ([fundamental-statements.service.ts](src/server/services/fundamental/fundamental-statements.service.ts)), plus a one-shot idempotent enrichment [scripts/fundamental-enrich-balance.ts](scripts/fundamental-enrich-balance.ts) (`job:fundamental-enrich`) filling those columns on existing rows where null (write-once preserved for existing figures). **Identities tie by construction**: EBITDA = OpInc + D&A, FCF = CFFO + CAPEX from stored derived values; margins = line/revenue same column; displayed Enterprise Value = exact sum of its displayed bridge components. **Number formatting** auto-scales Thousands/Millions/Billions so no figure exceeds 6 integer digits (0-dp statement $, 2-dp per-share, 1-dp %) — [format-statement.ts](src/lib/fundamental/format-statement.ts). Pure builder [financials.ts](src/lib/fundamental/financials.ts); read service [fundamental-financials.service.ts](src/server/services/fundamental/fundamental-financials.service.ts) + `/api/analysis/fundamentals/financials` route + `fundamentalsFinancialsQuery` schema. **Live-validated** on an enriched slice (ticker A): EV bridge ties exactly across all period + Current/LTM columns, per-period market cap from FMP key-metrics, annual aggregation + estimate column confirmed. **New tests** [tests/fundamental/financials.test.ts](tests/fundamental/financials.test.ts) (11). **`npm test` 648** (was 630; +11) **· `npx tsc --noEmit` clean · `prisma db push` clean.** **Engine 2 — Fundamental-Inflection Discovery (2026-06-27):** Second cleanly-separable engine (FMP-backed) detecting business-level inflections from statement history, shipped end-to-end behind the new `/fundamentals` module tab. **Boundary:** discovery + diligence (quality screens kill traps), no trade-recommendation logic. **Extends Engine 1** rather than forking — same append-only `(ticker, snapshotDate)` weekly key, reuses `RevisionReference` (universe + sector taxonomy), `RevisionGroupType`, and the `src/lib/revision` `scoring`/`aggregate` primitives. **Phase 0 validated** against the live key via read-only [scripts/fundamental-fmp-validate.ts](scripts/fundamental-fmp-validate.ts): 32 quarters (~8yr) of income/balance/cash-flow/ratios/key-metrics on mega + small-caps; **OCF + capex 32/32 on every probed small-cap** (the accruals trap-detector's inputs are safe — the one front-loaded coverage risk, cleared); FMP gross-margin / FCF / EV reconcile EXACTLY to our line-item formulas, but FMP's `ebitda` field diverges STRUCTURALLY on small/loss-making names (AXGN −2271%, GERN +4157%) — so EBITDA (and EBITDA-margin / net-debt-to-EBITDA) are computed OUR way (= operating income + D&A) and stored, ROIC/ROE trusted from FMP, raw FMP retained in JSON. **Provider** [src/infrastructure/providers/fmp/](src/infrastructure/providers/fmp/): added [statements.ts](src/infrastructure/providers/fmp/statements.ts) (merges income/balance/cash-flow per fiscal period), [ratios.ts](src/infrastructure/providers/fmp/ratios.ts), [quote.ts](src/infrastructure/providers/fmp/quote.ts) + normalized types — the stable field-mapping layer (signal code never sees FMP field names). **Storage** ([schema.prisma](prisma/schema.prisma), `db push` clean): `FundamentalPeriod` (as-first-reported fiscal facts + derived EBITDA/FCF/margins, write-once via `createMany skipDuplicates` over the existing-date set, never overwritten — new `FundamentalProvenance` **BACKFILL/LIVE** flag: pre-launch BACKFILL rows are restated-basis so their `firstSeenSnapshotDate` is intentionally fictional and the restatement→AuditLog detection only binds for an existing LIVE row whose figures later change), `FundamentalSnapshot` (weekly TTM leading metrics + current valuation multiples priced at snapshot + compact trailing series for sparklines), `FundamentalScore` (computed on the same key by a distinct service), `FundamentalSectorAggregate`, `DiscoveryQueueSnapshot`. **Signals** [src/lib/fundamental/](src/lib/fundamental/) (pure): `inflection.ts` (margin/FCF inflection = recent-slope − prior-slope, revenue-growth acceleration = slope of YoY-growth series, ROIC trend, deleveraging = −slope(netDebt/EBITDA)), `quality.ts` (Sloan accruals (NI−OCF)/avgAssets + NI-vs-CFO divergence → trap flag; compounder = mean-ROIC × consistency), `valuation.ts` (intra-ticker percentile vs own history — NOT cross-sectional), `series.ts` (TTM smoothing removes seasonality, YoY growth). **Ingestion** [src/server/services/fundamental/](src/server/services/fundamental/): statements service (`buildTickerFundamentals` map+derive, `persistPeriods` write-once + LIVE restatement detection), weekly orchestrator reusing `loadActiveUniverseTickers` + `fmpPool` (`--backfill` ⇒ BACKFILL provenance + ~9yr quarterly; routine ⇒ LIVE tail). **Scoring** [fundamental-scoring.service.ts](src/server/services/fundamental/fundamental-scoring.service.ts) reads the FULL backfilled `FundamentalPeriod` history so the discovery screen is **populated day one** (the explicit launch-blocker caveat — verified non-empty on first run): subsector z-score cross-sectional inflection signals (sector fallback), intra-ticker valuation percentile, equal-weight composite + deciles + week-over-week new-arrival, trap/compounder flags; writes Score + SectorAggregate + DiscoveryQueue. **API** `/api/analysis/fundamentals/` (discovery/diligence/overlap reads; admin-guarded `ingest`). **UI**: new **Fundamentals** tab in [BloombergModuleTabs.tsx](src/components/analysis/BloombergModuleTabs.tsx) → [FundamentalsClient](src/components/analysis/fundamentals/FundamentalsClient.tsx) sub-tabs (Discovery Rank, Margin-Inflection dumbbell, Quality/Value 2×2, Accruals diverging-bar screen, Compounder scatter, per-name Diligence, Engine1×Engine2 Overlap — the discovery payload powers the first five as projections of one cached read). CLI [scripts/fundamental-weekly.ts](scripts/fundamental-weekly.ts) (`job:fundamental`) + `.ps1` + [register-fundamental-task.ps1](scripts/register-fundamental-task.ps1) (Saturday 09:00, staggered after the revision job). **Backtest** [scripts/fundamental-backtest.ts](scripts/fundamental-backtest.ts) (`job:fundamental-backtest`) is labelled **DIRECTIONAL ONLY** — the backfill is restated-basis so any backtest on it inherits restatement look-ahead bias (a name that later restated shows corrected numbers it didn't have then); rigorous, leakage-free, survivorship-free fundamentals backtesting needs a point-in-time vendor (e.g. Sharadar) that slots into the same schema as a bolt-on, not a rebuild. True as-first-reported history only accrues forward from launch via the weekly LIVE snapshots. **Live-validated** on a 20-name slice: 20 snapshots → 685 periods inserted → 20 scored (5 new arrivals, 5 traps, 9 sectors / 17 subsectors) → discovery payload confirmed non-empty with real composites/deciles/cheapness/margins/compounder scores + a 36-quarter diligence series. **New tests** [tests/fundamental/](tests/fundamental/) (28: inflection, quality, valuation, series). **`npm test` 630** (was 599; +31) **· `npx tsc --noEmit` clean · `prisma db push` clean.** Two pre-existing global-scope CLI probes ([revision-fmp-validate.ts](scripts/revision-fmp-validate.ts) + the new fundamental one) were made `export {}` modules to avoid a tsc global-scope name collision. **Master Rank sourced from the market-map universe (2026-06-27):** The Analyst Revision Detector's revision reference now defaults to the user's **saved market-map universe** (`Universe`/`UniverseConstituent`/`Security`, ~2,000 active tickers) instead of the FMP company screener, so Master Rank covers every managed ticker rather than the top-25-by-market-cap signature of a `maxUniverse:25` smoke run. New `buildReferenceFromMarketMap({ universeId?, log })` in [reference-ingest.service.ts](src/server/services/revision/reference-ingest.service.ts) resolves the universe via `getOrCreateDefaultUniverse`, loads active constituents, maps each via the pure `marketMapConstituentToReference` helper (`security.ticker`→ticker, `security.name`→companyName, `constituent.sector`→sector, `constituent.subTheme`→subsector [peer-group key, falls back to sector under `MIN_PEERS`], latest `SecurityFundamentals.marketCap` when present), upserts into `RevisionReference`, then **reconciles** (deactivates refs whose ticker left the market-map set; skipped on an empty universe so a transient empty read can't wipe the reference). [revision-weekly-job.service.ts](src/server/services/revision/revision-weekly-job.service.ts) gained `referenceSource?: "MARKET_MAP" | "FMP_SCREENER"` (default `MARKET_MAP`) + optional `universeId`, branching Step 1 between the new builder and the legacy `refreshRevisionReference`; Steps 2–5 (`loadActiveUniverseTickers` → Leg A/B → earnings → snapshots → scoring) are unchanged — **FMP still supplies the revision data, only the ticker list changed**. [scripts/revision-weekly.ts](scripts/revision-weekly.ts) defaults to `MARKET_MAP` with a `--screener` opt-back-in flag; [researchIngestBody](src/lib/api/schemas.ts) gained `referenceSource`/`universeId` (the admin "Run weekly ingest" route already forwards `parsed.data`, so the button uses the new default). Coverage note: the job skips a ticker only when FMP returns neither estimates nor ratings; class-share/foreign symbol normalization (e.g. `BRK.B`, `MC.PA`) is a flagged follow-up, not blocking. **New tests** [tests/revision/reference-market-map.test.ts](tests/revision/reference-market-map.test.ts) (3: field mapping/uppercase-trim, blank→null, null-cap preserved). **`npm test` 602 · `npx tsc --noEmit` clean.** Operational next step: run one full `npm run job:revision` (preferred over the route to avoid the serverless timeout) so the queue grows from 25 to ~the active-ticker count. **Engine 1 — Analyst Revision Detector (2026-06-27):** New cleanly-separable engine (FMP-backed) detecting analyst revision inflections, shipped end-to-end behind the new `/research` module. **Boundary:** narrows + informs (research queue), no trade-recommendation logic. **Phase 0 validated** against the live key via read-only [scripts/revision-fmp-validate.ts](scripts/revision-fmp-validate.ts): key valid; bulk-CSV delivery works (Ultimate-class); Leg A estimates populated mega + small-cap (`revenue/ebitda/ebit/netIncome/sga/eps` low-avg-high + counts — no OCF/capex/FCF, absolute dollars); Leg B deep + backtestable (`/grades` event-level 2012→present, `/grades-historical` monthly distribution, `/price-target-news` event-level, `price-target-consensus` high/low/median/consensus). **Storage foundation** ([schema.prisma](prisma/schema.prisma), `db push` clean): append-only point-in-time store keyed `(ticker, snapshotDate)`, never overwritten — `RevisionReference` (ticker/CIK/sector/subsector/identifiers; reusable by a future Engine 2), `RevisionSnapshot` (both legs; typed leaders + spotty `estimatesJson`/`ratingsJson`), `RatingEvent` + `PriceTargetEvent` (Leg B backtest history; `createMany skipDuplicates`), `RevisionSectorAggregate`, `RevisionScore` (computed on the same key by a distinct service so signal logic can change without touching ingestion), `ResearchQueueSnapshot`; new `RevisionGroupType` enum. **Provider** [src/infrastructure/providers/fmp/](src/infrastructure/providers/fmp/): `fmp-client.ts` (key from [env.ts](src/infrastructure/config/env.ts) `FMP_API_KEY`/`FMP_TIER`/`FMP_CALLS_PER_MINUTE`, retry/backoff on 401/403/429/5xx, `fmpPool` worker pool honoring the per-minute budget, JSON + CSV helpers) + per-symbol fetchers (estimates/grades/price-targets/earnings-calendar/screener/profile/EOD) + bulk-CSV helpers. **Ingestion** [src/server/services/revision/](src/server/services/revision/): reference/universe builder (FMP screener, US major-exchange, market-cap floor, ~2,500-3,000 names, `maxUniverse` cap for staged/smoke runs), Leg A weekly snapshot (per-symbol), Leg B consensus (per-symbol pool — **chosen over the global bulk file because FMP throttles non-profile bulk to ~1/10s and it paginates into many slow parts**) + event backfill, earnings calendar (proximity weighting), and the weekly orchestrator merging both legs into one append-only snapshot per ticker. CLI [scripts/revision-weekly.ts](scripts/revision-weekly.ts) (`job:revision`, flags `--backfill`/`--no-reference`/`--enrich`/`--limit=`/`--date=`) + `.ps1` wrapper + [register-revision-task.ps1](scripts/register-revision-task.ps1) (Saturday weekly Scheduled Task). **Signals/scoring** [src/lib/revision/](src/lib/revision/) (pure): Leg A breadth/slopes/dispersion, Leg B net-rating momentum + PT revision, proximity-to-earnings weighting; peer-relative z-score within subsector (sector fallback when subsector < ~8-10 names), equal-weight composite, deciles, week-over-week new-arrival flag; [revision-scoring.service.ts](src/server/services/revision/revision-scoring.service.ts) writes `RevisionScore` + `RevisionSectorAggregate` + `ResearchQueueSnapshot`. **Backtest** [scripts/revision-legb-backtest.ts](scripts/revision-legb-backtest.ts) (`job:revision-backtest`): IC + quantile spread of Leg B vs forward EOD returns to inform initial leg weighting (Leg A has no history yet → composite stays equal-weight). **API** `/api/analysis/research/` (queue/trajectory/rotation/heatmap reads; admin-guarded `ingest`). **UI**: new **Research** tab in [BloombergModuleTabs.tsx](src/components/analysis/BloombergModuleTabs.tsx) → [ResearchClient](src/components/analysis/research/ResearchClient.tsx) (Master Rank + per-signal sparkbars, Revision Trajectory, Rotation Flow, Breadth Heatmap). **Live-validated** on a 25-name slice: reference → Leg A 25/25 → Leg B 25/25 → 25 snapshots → 17,764 rating + 2,184 PT events → scoring (8 sectors / 15 subsectors) → backtest. Also fixed a pre-existing unrelated tsc error in [scripts/profile-market-map.ts](scripts/profile-market-map.ts) (`code: string` → `BenchmarkCode`) so the typecheck gate is clean. **New tests** [tests/revision/](tests/revision/) (31: signals, scoring, aggregate/peer-fallback, backtest). **`npm test` 599** (was 542; +57) **· `npx tsc --noEmit` clean · `prisma db push` clean.** **Server-side live market-map refresh (2026-06-27):** Replaced the admin-browser-driven 60s `mode=tail` ingest with a server-side REGULAR-hours runner so the grid is correct at the open for every viewer (and at ~2000 tickers) without any browser open. **Root cause:** the grid's `D1` is close-to-close from `PriceHistory`; at the open the latest bar is still yesterday's close, so `D1` showed yesterday until the admin tab's per-ticker tail ingest crawled through the universe (minutes at ~2000 tickers, writing ticker-by-ticker, so the grid self-corrected through the morning — and never refreshed at all with no admin tab open). **Approach (cache-integrated overlay, spark-only):** new bulk provider [yahoo-bulk-quote.ts](src/infrastructure/providers/yahoo-bulk-quote.ts) `fetchYahooBulkQuotes` fetches Yahoo's anonymous `/v8/finance/spark?symbols=...&range=1d&interval=1d` in ~50-symbol chunks (so ~2000 tickers = ~40 requests vs ~2000), parsing the flat `{ SYM: { close[], chartPreviousClose, previousClose, timestamp[] } }` shape into `{ price, prevClose, asOfUnix }`; retry/backoff on 401/429/5xx, never throws (per-chunk misses accumulate into `failed`), keys by INPUT ticker via a `toYahooSymbol` reverse map, and reports `servedVia` (`spark` today — the v7 `quote` cookie/crumb fallback is a **documented follow-up, not built**; v7 confirmed 401 anonymously). New snapshot service [live-regular.service.ts](src/server/services/live-regular.service.ts) (`globalThis.__liveRegularSnapshot`, `getLiveRegularSnapshot`, `sweepRegularQuotes(db)` storing `{ price, prevClose, asOfUnix, tradeDateEt }`). New pure overlay `applyLiveRegularOverlay(series, quote, mode)` in [market-map.service.ts](src/server/services/market-map.service.ts) (date-anchored on the quote's ET trade date, never wall-clock): append when the DB lags ≤3 calendar days; same-date is **replace** in `"live"` mode (live price supersedes a seeded partial bar) and **no-op** in `"frozen"` mode (the official EOD close wins); future-bar / >3d gap skip. Unlike the extended overlay it keeps the close-to-close chain intact so `securityHorizonMetrics` recomputes every horizon (`D1 = price/prevClose`). `computeAndCacheMarketMap` ([market-map-cache.service.ts](src/server/services/market-map-cache.service.ts)) gained an `options` param so the overlay is **baked into the same `MarketMapSnapshot` cache row** — warm reads stay sub-second on the route's existing fast path (no per-request live compute). New runner [regular-runner.ts](src/server/services/regular-runner.ts) (started from [instrumentation.ts](src/instrumentation.ts), singleton, 60s, in-flight-guarded): during REGULAR it sweeps + rewrites the `RETURN/SP500` cache per universe with `liveMode:"live"`; on the first tick after leaving REGULAR it does **one freeze sweep + one `liveMode:"frozen"` cache write** to capture the regular close as today's bar, then stops — **no clear** (closes the 4pm reversion-to-yesterday gap until the daily job's `refreshUniverseTail` writes the official close and `precomputeAllMarketMaps` restores the clean tape). The market-map loop was **removed from [snapshot-refresh-runner.ts](src/server/services/snapshot-refresh-runner.ts)** so it no longer clobbers the overlay with static close-to-close data (factor refreshes stay; the regular runner now owns the market-map cache during REGULAR). The [market-map route](src/app/api/universes/[id]/market-map/route.ts) gained a `live:{ applied, mode, asOf, tickerCount, servedVia }` monitoring block (early-warning if spark ever fails) and passes `liveQuotes`/`liveMode` into the non-cacheable (filtered / non-COMPANY) live-compute branches; the cacheable COMPANY fast path is unchanged (cache already overlaid). Client [MarketMapPageInner.tsx](src/app/(analysis)/market-map/MarketMapPageInner.tsx) dropped the 60s `mode=tail` interval and the on-mount tail (now `["missing"]` only) — intraday 1D is server-owned; EOD closes land via the daily job + `maybeRunStartupCatchUp`. **Single-instance constraint (documented):** `globalThis.__liveRegularSnapshot` is per-process (same as `__extendedHoursSnapshot`) — valid for the one 24/7 desktop process behind the Cloudflare tunnel; multi-instance/serverless would need a shared store (e.g. Redis). **New tests** [tests/market/yahoo-bulk-quote.test.ts](tests/market/yahoo-bulk-quote.test.ts) (spark flat/legacy parse, chunking, 401 retry/backoff, `servedVia`, `failed` accumulation) + [tests/market/live-regular-overlay.test.ts](tests/market/live-regular-overlay.test.ts) (append/replace/frozen-no-op/future-bar/stale-gap/NaN/all-horizon recompute). **`npm test` 568 · `npx tsc --noEmit` clean.** **Production build gate + precomputed snapshot read-path (2026-06-25):** Made `next build && next start` the performance source of truth and eliminated per-request recomputation on the four heaviest GET surfaces. **Build unblocked:** the only blocking ESLint error was an unescaped apostrophe in [DataClient.tsx](src/components/analysis/data/DataClient.tsx) (`won't` → `won&apos;t`); `npm run build` now passes clean (`npx tsc --noEmit` clean, `npm test` **542**). **Snapshot read-path (the institutional gap):** four new JSON-blob Prisma models — `MarketMapSnapshot` (`@@unique([universeId, metric, benchmark])`), `FactorExposureGridSnapshot` + `FactorAttributionSnapshot` (both `@@unique([portfolioId, model, regressionWindow])`), `FactorPerformanceSnapshot` (`@@unique([metric, benchmark])`) — each fronted by a cache service mirroring [factor-rolling-cache.service.ts](src/server/services/factor-rolling-cache.service.ts): [market-map-cache.service.ts](src/server/services/market-map-cache.service.ts), [factor-exposure-cache.service.ts](src/server/services/factor-exposure-cache.service.ts), [factor-attribution-cache.service.ts](src/server/services/factor-attribution-cache.service.ts), [factor-performance-cache.service.ts](src/server/services/factor-performance-cache.service.ts) (`read*` / `write*` / `computeAndCache*` / `precomputeAll*`). The exposure snapshot-building logic was lifted out of the route into `buildFactorExposureSnapshot` so the route's cold path and the precompute produce byte-identical payloads. **Routes are now read-first** ([market-map](src/app/api/universes/[id]/market-map/route.ts), [exposure](src/app/api/analysis/factors/exposure/route.ts), [attribution](src/app/api/analysis/factors/attribution/route.ts), [performance](src/app/api/analysis/factors/performance/route.ts)): serve the cached blob; a cold miss computes live + writes through so the next read is warm. The **market-map fast path is gated** to COMPANY rowLevel with no extended-hours overlay and no sector/sub-theme filter — the overlay and filtered/non-COMPANY paths stay live (they are inherently dynamic); the client always fetches COMPANY and aggregates sectors/sub-themes in-browser, so COMPANY is the only level cached. **Daily job extended** ([factor-daily-precompute.service.ts](src/server/services/factor-daily-precompute.service.ts)): Step 4 became a combined per-portfolio loop running `runFactorEngine` **once** per portfolio × (model, window) and feeding all three snapshots from that single result (rolling betas + exposure + attribution — `computeFactorAttribution` gained an optional `precomputedEngine` param so it no longer re-runs the engine); new Step 5 (market maps, every universe × metric × benchmark) and Step 6 (factor-performance, every metric × benchmark). **Drift-snapshot persistence + alert evaluation moved off the hot GET path** onto the daily cadence — they now run once per (portfolio, model) at the standard 252d window inside the precompute loop (`persistFactorSnapshot` + `evaluateFactorAlerts`), since a warm exposure GET no longer runs the engine; this is the correct daily cadence for drift detection and removes a side-effecting write from a read. **Market-hours runner** [snapshot-refresh-runner.ts](src/server/services/snapshot-refresh-runner.ts) (started from [instrumentation.ts](src/instrumentation.ts), singleton, in-flight-guarded) recomputes the hot set every 60s during **REGULAR** only — market-map RETURN×SP500 per universe, factor-performance RETURN×SP500, and exposure+attribution per portfolio at MACRO14/252 (one engine run feeds both) — so warm reads are at most ~60s stale intraday; off-hours it idles (daily job + cold-miss cover everything). **Dead debug removed:** the `#region agent log` blocks that POSTed to a now-dead `127.0.0.1:7864` on every overlay request/anomaly were deleted from both the market-map route and [market-map.service.ts](src/server/services/market-map.service.ts) (the one debug flag that fed real ranking logic, `_dbgApplied`, was preserved as `overlayApplied`). **Admin ingest dedup:** new [ingest-inflight.ts](src/server/services/ingest-inflight.ts) (`globalThis` in-flight `Set` + `withIngestLock(key, fn)`) wraps the universe + benchmark ingest routes keyed `universe:<id>:<mode>` / `benchmark:<mode>`, so multiple admin tabs firing the first-visit + 60s live tail no longer stampede N overlapping ~1,229-ticker Yahoo sweeps (a concurrent call returns `{ deduped: true }`). **Dev quick wins:** [next.config.ts](next.config.ts) `onDemandEntries` → `pagesBufferLength 8` / `maxInactiveAge 300_000` (recently-visited module tabs stop recompiling on every switch); `npm run dev` now uses `--turbopack` (webpack fallback at `dev:webpack`); [precompute-runner.ts](src/server/services/precompute-runner.ts) defers the startup catch-up by `PRECOMPUTE_STARTUP_DELAY_MS` (default **180s**, `0` = immediate) so the boot ingest no longer contends with first navigation. **Production validation (warm, no contention):** market-map **178–240ms**, performance **8–88ms**, exposure **9–81ms** — all sub-second; attribution **~1.2s**, bounded by its **3.5MB** full-history daily-array payload (JSON-serialization cost, not recompute — still ~10× faster than the prior ~12s live compute). **Follow-up noted:** attribution warm latency could drop below 1s by storing/serving the blob as a raw JSON string (skip the Prisma parse + `NextResponse.json` re-stringify) or by trimming the full-history arrays — deferred as a separate optimization. **MarketMap performance + Top Factor Movers blackout fixes (2026-06-25):** Removed the app-wide tab-switch lag and the periodic "blackout" of the Top Factor Movers panel. **Blackout root cause + fix:** [FactorTopMoversTable.tsx](src/components/FactorTopMoversTable.tsx) had `reloadToken` in its React Query key, and the market-map page bumped `reloadToken` every 30s; in TanStack Query v5 a new key is a new query with `data === undefined`, so the panel reset to `[]` and rendered blank over the near-black canvas every 30s (sibling tables survived because they use `useState`+`useEffect`). Dropped `reloadToken` from the query key, added `placeholderData: (prev) => prev`, kept the existing session-aware `refetchInterval` (30s REGULAR / 5min otherwise) as the sole refresh, and removed the now-dead `reloadToken` prop + its call site in [MarketMapClient.tsx](src/components/MarketMapClient.tsx). **Refetch storm:** [providers.tsx](src/components/analysis/providers.tsx) `QueryClient` now sets `refetchOnWindowFocus: false` (previously defaulted to true; alt-tabbing refetched every mounted query). **Session-aware polling (tight during REGULAR, relaxed off-hours):** Overview holdings query [OverviewClient.tsx](src/components/analysis/overview/OverviewClient.tsx) `refetchInterval` is now `REGULAR ? 20s : 60s`; the market-map grid auto-refresh in [MarketMapPageInner.tsx](src/app/(analysis)/market-map/MarketMapPageInner.tsx) became a self-scheduling `setTimeout` choosing `REGULAR ? 30s : 60s` (new `AUTO_REFRESH_OFFHOURS_MS`), re-evaluating the session each tick (keeps the visibility guard + `visibilitychange` immediate bump). **Server-side ranked-result cache:** [factor-top-movers.service.ts](src/server/services/factor-top-movers.service.ts) `getFactorTopMovers` re-read the ~1,200-row grid blob and re-ranked 14 factors every request; added a `globalThis`-singleton `Map` cache keyed `horizon|mode|window|limit` (same dev-bundle-sharing pattern as the Prisma client), TTL `30s` for live 1D during REGULAR / `5min` otherwise (5D+ static EOD self-corrects after a precompute) + `_resetFactorTopMoversCache()`. **Lazy-mount heavy panels:** new reusable [DeferUntilVisible.tsx](src/components/analysis/shared/DeferUntilVisible.tsx) (IntersectionObserver, reserves placeholder height, mounts once) wraps each per-holding Recharts tile in [HoldingsLiveChartGrid.tsx](src/components/analysis/overview/HoldingsLiveChartGrid.tsx) (a large portfolio no longer instantiates dozens of `ResponsiveContainer`s on first paint) and the below-the-fold polling sections in [DataClient.tsx](src/components/analysis/data/DataClient.tsx) (`DataSourceStatus` 30s, `SecuritiesHealth`/`UsersPanel` 60s, `AuditLogTable`) so editing a position at the top no longer kicks off those polls. **Selector-based Zustand:** converted whole-store `useAnalysisStore()` destructuring to per-field selectors in [TopBar.tsx](src/components/analysis/TopBar.tsx), [PerformanceClient.tsx](src/components/analysis/performance/PerformanceClient.tsx), [OverviewClient.tsx](src/components/analysis/overview/OverviewClient.tsx), and [FactorsClient.tsx](src/components/analysis/factors/FactorsClient.tsx) (these previously re-rendered on any store change). **Table collapse + row memoization:** [FactorTopMoversTable.tsx](src/components/FactorTopMoversTable.tsx) panels are now collapsed by default with a per-panel chevron + an Expand-all/Collapse-all control (the ~560 rows / ~3,360 cells no longer all mount up-front), and each row is a memoized `MoverRow` with its `heatmapRgb`/`sectorColor`/`subThemeColor` math behind `useMemo` so selection changes only re-render the rows whose selected state flipped. **Validation:** `npm test` **542** · `npx tsc --noEmit` clean. The full `next build` was intentionally **not** used as the gate — it is red on pre-existing lint debt (`prefer-const` / `react/no-unescaped-entities`) in files unrelated to this work ([LivePriceChartTile.tsx](src/components/analysis/overview/LivePriceChartTile.tsx), [FactorTooltip.tsx](src/components/analysis/factors/shared/FactorTooltip.tsx), [DataClient.tsx](src/components/analysis/data/DataClient.tsx), [yahoo-chart-http.ts](src/infrastructure/providers/yahoo-chart-http.ts), [multicollinearity.ts](src/lib/factors/market/multicollinearity.ts)); fixing those is out of scope for a perf change. Validated instead via types + tests (green) + a dev smoke test (`GET /market-map 200`, `GET /api/analysis/factors/top-movers 200`). **Dev hygiene reminder (cost us an Internal Server Error this session):** never `Remove-Item -Recurse .next` or run `next build` while a `next dev` is live — they clobber the same `.next` (recover by stopping dev + build, clearing `.next`, restarting `npm run dev`). **Portfolio cash holdings (2026-06-24):** Users can add a dollar-denominated **Cash** row to any portfolio from the Data tab (Equity/Cash toggle + Amount field). `PortfolioPosition` now has `isCash`, `cashAmount`, nullable `securityId`; one cash row per portfolio (service upsert). Cash earns **0% return** (0 vol/beta — dry-powder drag) and flows through `loadPortfolioWeights` ([portfolio.service.ts](src/server/services/portfolio.service.ts)), factor-engine `loadPortfolioReturns` (flat $1 coverage map in [factor-engine.service.ts](src/server/services/factor-engine.service.ts)), PnL/allocation ([pnl.service.ts](src/server/services/pnl.service.ts)), risk/concentration/performance/stress, holdings dashboard, and live-1D (Yahoo skipped). API: `POST /api/analysis/portfolio/positions` `{ isCash: true, cashAmount }`; PATCH `cashAmount`. **New tests** [tests/portfolio/cash-position.test.ts](tests/portfolio/cash-position.test.ts) + cash-drag case in [factor-portfolio-coverage.test.ts](tests/analysis/factor-portfolio-coverage.test.ts). **`npm test` 522 · `npx tsc --noEmit` clean · `prisma db push` clean.** **Extended-hours overlay on the market map + CLOSED-carryover + startup backfill (Pre-market / Live / After-hours / Closed, 2026-06-22):** The Performance top-bar status went from 2-state (Open/Closed) to 4-state via a pure clock heuristic in [src/lib/market-map/market-session.ts](src/lib/market-map/market-session.ts): `getUsMarketSession(now)` returns `PRE` (04:00-09:30 ET), `REGULAR` (09:30-16:00 ET), `POST` (16:00-20:00 ET), or `CLOSED`, all weekday-only via `Intl.DateTimeFormat("en-US", {timeZone: "America/New_York"})`. [MarketMapPageInner.tsx](src/app/(analysis)/market-map/MarketMapPageInner.tsx) renders the new chip with `Live` (light green `--color-positive`) keeping the LIVE badge during REGULAR, `Pre-market` (orange `--color-accent`), `After-hours` (dark red `#8b1f1f`), and `Closed` (muted secondary) — the regular-hours admin tail-ingest still triggers on REGULAR only. During PRE/POST sessions the grid automatically overlays the latest pre/post-market price as today's series endpoint and **recomputes every horizon column** (1D, 5D, 1M, 3M, 6M, 1Y) so the after-hours move surfaces everywhere, not just 1D. A minimal `Revert to close` / `Show after-hours` (or `Show pre-market`) ghost button appears in the controls strip during PRE/POST clock sessions AND during CLOSED whenever the server still holds a usable snapshot from the prior POST/PRE sweep, so users can keep flipping between the close-based grid and the most recent after-hours move overnight and over weekends; hidden during REGULAR and during CLOSED with no snapshot (where the toggle would be a no-op). The toggle's label, accent colour, and "as of HH:MM" hint follow the **snapshot session** (not the clock), so CLOSED-after-POST still reads "Show after-hours" in dark red. **Sweep runs server-side, not in the admin browser**: the 24/7 desktop hosts the Next.js process but the admin's browser is not always open, so an admin-tab-driven sweep would silently go stale for the other 10 viewers. New singleton runner [extended-hours-runner.ts](src/server/services/extended-hours-runner.ts) started from [src/instrumentation.ts](src/instrumentation.ts) wakes every 60s and only fires `sweepExtendedHours` ([extended-hours.service.ts](src/server/services/extended-hours.service.ts)) when the current session is PRE or POST. **Snapshot lifecycle (refined)**: REGULAR clears the snapshot (live regular-session prices supersede); PRE/POST replace it wholesale every sweep (so tomorrow's PRE never displays yesterday's POST data); **CLOSED preserves a populated snapshot** so a user opening the page at 10pm Monday still sees Monday's last after-hours move and Saturday browsers still see Friday's; **CLOSED with an empty snapshot triggers a one-shot BACKFILL** that uses Yahoo `range=5d` (vs the usual `range=1d`) to reach back through overnight gaps and weekends to the most recent POST print, so a server restart at any point during CLOSED recovers visibility within ~30s without waiting for the next PRE sweep. The parser's session classifier was upgraded to fall back to ET-clock time-of-day (instead of always REGULAR) when Yahoo's `currentTradingPeriod` doesn't match a bar — this is what makes prior-day POST bars in a 5d query classify correctly. Backfill is gated by a 5-min cooldown to avoid hammering Yahoo on persistent outages. `asOf` for backfill snapshots is the actual latest bar timestamp, not "now", so the UI shows e.g. "After-hours as of Fri 7:55pm". The route adds a defensive `currentSession !== "REGULAR"` guard so live regular trading never serves a stale extended snapshot regardless of runner lag. The sweep loads active universe tickers and calls `fetchYahooExtendedQuotes` ([yahoo-chart-http.ts](src/infrastructure/providers/yahoo-chart-http.ts), Yahoo v8 chart `range=1d&interval=5m&includePrePost=true`) via a 5-worker pool with 150ms politeness gap; the pure parser `parseYahooExtendedQuote` classifies the latest non-null print using `meta.currentTradingPeriod.{pre,regular,post}` boundaries and returns `{price, session, asOfUnix, prevClose, regularClose}`. REGULAR prints are dropped from the snapshot (the regular close already lives in `PriceHistory`); only PRE/POST prints land in the in-memory ticker→price map. The pure overlay helper `applyExtendedOverlay(series, price, todayIso)` in [market-map.service.ts](src/server/services/market-map.service.ts) replaces the last bar in-place when `last.date === todayIso` and appends otherwise, keeping the daily-return chain intact; `computeMarketMap` accepts an optional `{ extendedPrices?: Map<ticker, number> }` and the API route [universes/[id]/market-map/route.ts](src/app/api/universes/[id]/market-map/route.ts) always inspects `getExtendedSnapshot()` for availability info but only applies the overlay when the client passes `?extended=1` AND the snapshot session is PRE/POST AND the price map is non-empty AND the current clock session is not REGULAR. The response carries `extended: { requested, applied, available, session, asOf, tickerCount }` — `available` is decoupled from the client's opt-in so the toggle can decide its own visibility, and `applied` reflects whether the overlay was actually used. **Extended-hours prices are never written to `PriceHistory`** — that table is the EOD `adjClose` tape and stays clean. **Snapshot lives on `globalThis`** — in Next.js dev mode `instrumentation.ts` (where the runner starts) and route handlers compile into **separate server bundles**, so a plain module-level `let current` in [extended-hours.service.ts](src/server/services/extended-hours.service.ts) was duplicated and the runner's writes were invisible to the route reader (the first runtime test showed `[extended-hours] backfilled 1162/1225 tickers (CLOSED startup)` in the server log while the route returned `{ available: false, tickerCount: 0 }`, hiding the toggle). The snapshot is now hoisted onto `globalThis.__extendedHoursSnapshot` via the same singleton pattern as the Prisma client in [infrastructure/db/client.ts](src/infrastructure/db/client.ts), so every bundle shares one instance. Verified post-restart: API returned `{ available: true, applied: true, session: "POST", tickerCount: 1162, asOf: "2026-06-22T23:55:59Z" }` and the dark-red `[Revert to close] · After-hours as of 06:59 PM` chip rendered on the controls strip. **New tests** [tests/market/market-session.test.ts](tests/market/market-session.test.ts) (12: weekday boundaries in winter EST + summer EDT across DST, weekend cases, `isExtendedSession`) + [tests/market/extended-overlay.test.ts](tests/market/extended-overlay.test.ts) (7: replace vs append semantics, immutability, NaN/Infinity no-op, plus `securityHorizonMetrics` integration showing recomputed 1D and 5D telescoping identities) + [tests/market/extended-quote-parse.test.ts](tests/market/extended-quote-parse.test.ts) (7: empty payload, all-null closes, POST/PRE/REGULAR classification via `currentTradingPeriod`, trailing-null skip, missing-period REGULAR fallback). **`npm test` 393** (was 335; +33 market + +25 misc test files already shipped) **· `npx tsc --noEmit` clean.** Future hardening would (a) add a US equity holiday calendar so the session label is not just a clock heuristic, (b) unify the regular-hours tail-ingest onto the same server-side runner so it stops depending on an admin tab, and (c) move to a paid market-data feed if institutional reliability is needed. **Rolling factor betas: precomputed rolling series + IPO-safe portfolio returns + compact coverage warning (2026-06-21):** Fixed the empty "Rolling Factor Betas" chart and the blank Exposure/Risk panels. **(1) IPO-safe portfolio returns:** [loadPortfolioReturns](src/server/services/factor-engine.service.ts) inner-joined prices across *every* holding, so one recently-listed stock collapsed the whole portfolio's aligned window below the `2k+30` minimum and `runFactorEngine` returned null — blanking Exposure, Risk, and Attribution. Now built from the **union** of position dates via the pure helper `buildCoverageWeightedReturns` ([portfolio-coverage.ts](src/lib/factors/regression/portfolio-coverage.ts)): each holding contributes only on dates it traded, present signed weights renormalize to full investment, dates below 50% gross coverage drop. Emits `PortfolioCoverageDiagnostics` naming the short-history/excluded holdings + dropped-date count. **(2) Rolling 252d series precomputed daily:** the engine already computed `rollingFits` every request but only used it for attribution; the chart read sparse per-page-view `FactorExposureSnapshot` rows. New `buildRollingBetaSeries` ([rolling-beta-series.ts](src/lib/factors/regression/rolling-beta-series.ts)) maps it to the chart shape; new `FactorRollingBetaSnapshot` model (`@@unique([portfolioId, model, regressionWindow])`) + [factor-rolling-cache.service.ts](src/server/services/factor-rolling-cache.service.ts) persist it; `precomputeAllPortfolioRollingBetas()` runs as **Step 4** of the daily job ([factor-daily-precompute.service.ts](src/server/services/factor-daily-precompute.service.ts)) over every portfolio × the 4 HORIZON windows; each daily run re-fits the trailing window so the series drops the oldest day and adds the newest. `/api/analysis/factors/exposure/history` is now window-aware + cache-first with live write-through; dead snapshot-based `getExposureHistory` removed. **(3) Compact coverage warning:** replaced the full-width banner in [FactorsClient.tsx](src/components/analysis/factors/FactorsClient.tsx) with a small discrete yellow chip ([CoverageWarning.tsx](src/components/analysis/factors/panels/CoverageWarning.tsx)) above the tab strip (spans Exposure/Risk/Attribution); hover lists the excluded holdings + dates. Coverage rides on both the success snapshot and the 422 body (latter enriched by lightweight `getPortfolioCoverageDiagnostics`). **New tests** [factor-portfolio-coverage.test.ts](tests/analysis/factor-portfolio-coverage.test.ts) (5) + [factor-rolling-beta-series.test.ts](tests/analysis/factor-rolling-beta-series.test.ts) (3). **`npm test` 335 · `npx tsc --noEmit` clean · `prisma db push` clean.** **Dead EWMA regression path removed — single equal-weight OLS across both levels (2026-06-21):** Closed the latent asymmetry where the portfolio-level engine threaded an optional EWMA half-life through `multivariateOls` while the per-stock service used unweighted OLS. The half-life was permanently `null` in practice (store default `factorEwHalfLife: null`, no UI control ever calls `setFactorEwHalfLife`, the [FactorsClient](src/components/analysis/factors/FactorsClient.tsx) URL builder only appended `&ew=` when truthy, and `exponentialWeights(n, null)` returned all-1s — bit-identical to unweighted), so the divergence was infrastructure-only. Removed the dead path so the codebase can never accidentally diverge again: deleted `src/lib/factors/regression/weights.ts`, dropped the `ewHalfLife` parameter from `rollingMultivariateOls` / `rollingResidualStream` / `computeHoldingsLoadings` and their consumers ([factor-engine.service.ts](src/server/services/factor-engine.service.ts), [factor-drivers.service.ts](src/server/services/factor-drivers.service.ts)), removed the `ew` query field from `factorQueryParams` ([schemas.ts](src/lib/api/schemas.ts)) and the three routes that destructured it (exposure / risk / drivers), removed `ewHalfLife` from `FactorEngineParams` ([types/factors.ts](src/types/factors.ts)), and removed `factorEwHalfLife` from the analysis store (state + setter + initial value + partialize; persist `version: 11 → 12` with a `delete next.factorEwHalfLife` migration so prior browsers shed the stale key cleanly). The `FactorsClient` URL helper and `factor-exposure` query key dropped the half-life arg. Removed the two now-orphaned `exponentialWeights` test cases ([factor-regression.test.ts](tests/analysis/factor-regression.test.ts)). The portfolio `endFit` (the static horizon fit feeding `endFit.betas` and the static-beta daily attribution) and the per-stock snapshot `fit` are now both `multivariateOls(y, x)` over the same horizon window — one estimator family, enforced by the type system. The `factorCovarianceMatrix(series, weights, annualize)` `weights` param is intentionally left in place: it is symmetric (always `null` at all 5 call sites across per-stock and portfolio paths) and removing it would churn a shared primitive without changing any output. Verification: `grep -E 'ewHalfLife|exponentialWeights'` returns zero hits outside the store migration; `npx tsc --noEmit` clean; **`npm test` 327** (was 331; net −4 from the deleted `exponentialWeights` cases, no value changes elsewhere). **Per-stock attribution unified on static horizon betas — grid ⇄ waterfall now tie (2026-06-21):** Closed the long-standing discrepancy where a stock's Alpha / Global-Equity / factor numbers in the **Portfolio Exposure grid** did not match the same stock's **detail-popup waterfall** (even at Attribution Period = 1Y). Root cause was three different estimators feeding the two surfaces: the grid used static-horizon-β × Σr (simple space) for factor cells but a separate 60d-**rolling**-OLS Σα/Σε for Alpha/Unexplained, while the popup waterfall summed the 60d-rolling daily β·x / α / ε series in **log** space — none reconciled, and the grid Alpha ignored `factorAttributionMode` entirely (always simple). **Decision (user-confirmed): one canonical estimator = static horizon betas.** A 60-obs, 14-factor (+intercept) collinear regression is too low-DOF to attribute per-factor returns from; the stable full-horizon fit is the right basis (and already what portfolio-level attribution uses). **Implementation:** new pure helper [computeStaticBetaPeriodSlice](src/lib/factors/attribution/static-beta-period.ts) (systematic = β_horizon×Σr per factor; alpha = α×obs; residual = Σy − systematic − alpha — the plug; identity closes by construction). [factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts) `periodSlices` now uses it in **both** simple and log space (replacing the rolling-restricted sums); new `returnByFactorLog` on `PerStockPeriodSlice` + `returnContributionLog` on `PerStockFactorCell` (static log-OLS β_log×Σln(1+r)). The [per-stock route](src/app/api/analysis/factors/per-stock/route.ts) overlay writes the log factor contribs onto cells and self-heals caches predating the new fields. **Grid readers made mode-aware** so log mode (the default) reads the log static fields: [PortfolioFactorGrid](src/components/analysis/factors/panels/PortfolioFactorGrid.tsx) (`pickValue`/`summaryValue`/`factorCellShownValue`/`buildTotalRow`/`compareRows` threaded with `mode`), [PerStockGrid](src/components/analysis/factors/panels/PerStockGrid.tsx) (`pickValue`/`renderFactorCell`), [stats.ts](src/lib/factors/screener/stats.ts) `factorCellValue`, and [sector-heatmap.ts](src/lib/factors/screener/sector-heatmap.ts) `aggregateBySectorFactor`. **Waterfall rebuilt from the snapshot** [PerStockDetail.tsx](src/components/analysis/factors/panels/PerStockDetail.tsx): the return waterfall now reads `row.periodSlices[period]` (the SAME numbers the grid shows) instead of the rolling time-series, so popup == grid by construction; "Total ≈" uses the slice's price-based `realizedTotalReturn`; the Static-alpha headline is relabelled as the annualized view of the same fit (α×252 vs the waterfall's α×days). The rolling-60d chart is demoted to an explicitly-labelled "rolling β drift" illustration that feeds NO headline/grid number (still drives the residual t-stat/CI band + portfolio residual stream). **New tests** [tests/analysis/factor-static-beta-period.test.ts](tests/analysis/factor-static-beta-period.test.ts) (9: period identity, β×Σr / α×obs correctness, residual-is-plug, grid==waterfall reconstruction in both modes, mode-routing). **`npm test` 331 · `npx tsc --noEmit` clean.** **Origin performance fixes + Cloudflare-Access multi-user/admin model (2026-06-20):** Addressed tunnel timeouts and added multi-user authorization over one shared master DB. **Fix 1 (non-blocking catch-up):** [precompute-runner.ts](src/server/services/precompute-runner.ts) `startPrecompute()` now **spawns `npm run job:daily` as a separate OS process** (stdout/stderr teed to a timestamped `logs/` file) instead of running the ~10-min regression chain inline and starving the web event loop for minutes after boot; `RunnerState` gained `pid`, `lastSummary` is always null. **Fix 2 (batched market map):** [market-map.service.ts](src/server/services/market-map.service.ts) replaced the per-ticker `loadRecentPrices` loop (~1,220 sequential queries) with a single `priceHistory.findMany` over all securityIds (≈600-day cutoff), grouped + sliced to the last 320 bars each. **Multi-user/admin:** new `User` model + `UserRole` enum + nullable `Portfolio.userId` ([schema.prisma](prisma/schema.prisma); `db push` + one-shot [scripts/backfill-portfolio-owners.ts](scripts/backfill-portfolio-owners.ts)). [auth.service.ts](src/server/services/auth.service.ts) verifies the Cf-Access JWT via `jose` (team certs + optional AUD; direct/local access trusted as admin; degrades to the unverified Access email header with a warning until `CF_ACCESS_TEAM_DOMAIN` is set), upserts the `User`, syncs role from `ADMIN_EMAILS`, throttles `lastLoginAt` ≤1/hr, and exposes `requireAdmin` / `assertPortfolioAccess` / `assertPositionAccess`; route guards in [guards.ts](src/lib/api/guards.ts) + [auth-response.ts](src/lib/api/auth-response.ts). Every portfolio CRUD route and every analysis endpoint taking a `portfolioId` is now **scoped to the current user** (403 on foreign/unknown id); admin-only mutations (universe constituents add/delete, universe + benchmark ingest, securities health, per-stock precompute, pipeline-refresh, data refresh) 403 for non-admins. New `GET /api/me` + admin-only `GET /api/admin/users`. UI gating (advisory; server enforces): [useMe.ts](src/lib/api/useMe.ts); market-map page hides Manage Tickers/Refresh + skips auto-ingest/live tail for non-admins; Data tab hides securities delist/live edits for non-admins + adds an admin-only Users panel. New env: `CF_ACCESS_TEAM_DOMAIN`, `CF_ACCESS_AUD`, `ADMIN_EMAILS`. New dep `jose`. **`npm test` 312 · `npx tsc --noEmit` clean**; runtime-verified `/api/me`, `/api/admin/users`, per-user portfolio scoping (200 owned / 403 foreign), and a non-blocking startup catch-up child process. **Period-driven portfolio decomposition + Total Return grid column + Factors-tab tooltips (2026-06-14):** Three changes. **(1) Period-driven decomposition**: the portfolio **Total Return Decomposition** and **Variance Decomposition** panels now actually respond to the Attribution Period control. Root cause was that `AttributionResult.daily` was derived from the rolling-OLS fit tail (gated by the burn-in), so when `n ≤ horizon window` it collapsed to a single point and every 1D…1Y slice showed the same numbers. Fix decouples the period series from the rolling burn-in: new `computeStaticBetaDailyAttribution` ([daily.ts](src/lib/factors/attribution/daily.ts)) + `computeStaticBetaDailyLogAttribution` ([daily-log.ts](src/lib/factors/attribution/daily-log.ts)) apply the horizon **end-fit** betas (`engineResult.endFit/endFitLog`) across the FULL aligned history (alpha = realized − systematic plug), defined for every date. [attribution.service.ts](src/server/services/attribution.service.ts) now ships `daily`/`dailyLog` = the static-beta series (feeds `periods`/`periodsLog` and the period-sliced variance via `pickPeriodRiskSummary`), while `cumulative`/`cumulativeLog` stay on the rolling series (time-series chart legitimately shows drifting betas). 1D variance fallback copy clarified in [PortfolioTotalsPanel.tsx](src/components/analysis/factors/panels/PortfolioTotalsPanel.tsx) (single obs ⇒ no realised variance; falls back to whole-window Euler). **(2) Total Return grid column**: [PortfolioFactorGrid.tsx](src/components/analysis/factors/panels/PortfolioFactorGrid.tsx) gained a leftmost period-driven **Total Return** summary column (signed heat, stat-invariant, same machinery as Alpha/Unexplained) matching the per-stock screener grid; per-holding cell = `row.realizedTotalReturn` (already overlaid to the period by the per-stock route), TOTAL row = the portfolio's realized period total via `pickPeriodSummary(attribution, period, mode)` (ties to the Total Return Decomposition headline) — `attribution`+`selectedPeriod` plumbed through `PortfolioExposureGridSection` in [FactorsClient.tsx](src/components/analysis/factors/FactorsClient.tsx). PerStockGrid already had the column. **(3) Factors-tab global tooltips**: every factor label AND derived metric (Total Return, R², Vol, Alpha, Unexplained, Market Beta, Top Factor Tilt, Factor Concentration, Systematic Risk) now has a concise hover popup with definition + how-calculated + a **Data used** provenance line. New `dataSource` field on every `FactorDef` ([factor-codes.ts](src/lib/factors/definitions/factor-codes.ts)) + a derived-metric registry [getMetricDef](src/lib/factors/definitions/metric-defs.ts); `FactorTooltip`/`InfoTooltip`/`MetricCard`/`Waterfall` extended with `dataUsed`; applied to both grids' summary headers, the variance + return waterfall segments, and the `HeaderSummary` metric cards. Factor headers in PerStockGrid/PortfolioFactorGrid/SectorFactorHeatmap inherit the data line automatically. **New tests** [tests/analysis/factor-static-beta-attribution.test.ts](tests/analysis/factor-static-beta-attribution.test.ts) (6: full-length identity, period-bucket distinctness regression guard, log reconciliation). **`npm test` 312 · `npx tsc --noEmit` clean.** **Factors Attribution Period + factor hover overviews + per-stock price chart (2026-06-13):** Made the **Attribution Period** control actually drive the panels and added it to the Per-Stock tab. New shared `resolvePeriodSlice(dates, label, refDate?)` in [period.ts](src/lib/factors/attribution/period.ts) (1D/5D = trailing obs count; 1M..1Y = calendar offset) + `pickPeriodSummary(attribution, period, mode)` in [pick-period-summary.ts](src/lib/factors/attribution/pick-period-summary.ts) (log → geometric headline + additive log bars; simple → arithmetic; cross-mode fallback). [PortfolioTotalsPanel.tsx](src/components/analysis/factors/panels/PortfolioTotalsPanel.tsx) + [HeaderSummary.tsx](src/components/analysis/factors/panels/HeaderSummary.tsx) use the picker (log-aware `headlineOverride`; explicit degraded copy). Per-Stock toolbar shows the period (`showPeriod` on [PerStockView.tsx](src/components/analysis/factors/panels/PerStockView.tsx)). The grid follows the period via precomputed `periodSlices` per row in [factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts) (Return = β×Σr over slice; Σα/Σε from the same rolling OLS restricted to the slice; simple+log), overlaid in the [per-stock route](src/app/api/analysis/factors/per-stock/route.ts) for the requested `period` (betas/risk/R²/vol stay on the horizon window; self-heals when a cached grid predates `periodSlices`). [PerStockDetail.tsx](src/components/analysis/factors/panels/PerStockDetail.tsx) waterfall slices to the period. **Factor hover overviews**: `FactorTooltip` gained a `concise` prop (first-sentence definition + full `howCalculated`), wired on all 14-factor headers in [PerStockGrid](src/components/analysis/factors/panels/PerStockGrid.tsx) / [PortfolioFactorGrid](src/components/analysis/factors/panels/PortfolioFactorGrid.tsx) / [SectorFactorHeatmap](src/components/analysis/factors/panels/SectorFactorHeatmap.tsx). **Per-stock price chart**: new [StockPriceChart.tsx](src/components/analysis/factors/panels/StockPriceChart.tsx) above the attribution waterfall — ranges 1D/5D/1M/6M/YTD/1Y/5Y/MAX (default 1D) + click-drag return measurement; hybrid [price-series.service.ts](src/server/services/price-series.service.ts) + [/api/analysis/securities/price-series](src/app/api/analysis/securities/price-series/route.ts) (live Yahoo intraday 1m/5m via new `fetchYahooIntraday` for 1D/5D, stored daily adjClose otherwise). **New tests** [tests/analysis/factor-period-slice.test.ts](tests/analysis/factor-period-slice.test.ts) (11). **`npm test` 291 · `npx tsc --noEmit` clean.** **Factors server-startup catch-up + freshness indicator (2026-06-13):** Added a safety net so saved per-stock regression grids are always current to the last completed trading close, even when the 17:00 Windows Scheduled Task was missed (PC off/asleep). On Next.js boot [src/instrumentation.ts](src/instrumentation.ts) fires-and-forgets [maybeRunStartupCatchUp](src/server/services/precompute-runner.ts), which checks freshness via [precompute-freshness.ts](src/lib/factors/diagnostics/precompute-freshness.ts) (`lastTradingClose(now)` = most recent weekday whose 17:00 local has passed; `stale` = any expected MACRO14×HORIZON row missing or older than the last close — primary signal is `computedAt`, not `asOfDate`, to avoid false-stale loops on KF/AQR publish lag) and only kicks off the full daily chain if stale. The full chain (price tail → factor pipeline → grid precompute for all 4 horizons) is now in a shared [factor-daily-precompute.service.ts](src/server/services/factor-daily-precompute.service.ts) consumed by both [scripts/daily-precompute.ts](scripts/daily-precompute.ts) (Scheduled Task) and the in-memory [precompute-runner.ts](src/server/services/precompute-runner.ts) (startup) — one code path. The runner exposes `RunnerState` (`idle | running | done | error`, `startedAt`, `finishedAt`, `lastTrigger`, `lastSummary`); [GET /api/analysis/factors/precompute-status](src/app/api/analysis/factors/precompute-status/route.ts) returns `{ freshness, runner }`. The Per-Stock toolbar's `cacheControls` in [PerStockView.tsx](src/components/analysis/factors/panels/PerStockView.tsx) now shows a coloured dot (green = current · amber = stale · pulsing accent = refreshing) + `Last saved {time} · as of {date}` (or `Refreshing… (startup catch-up | manual)`), polled at 5s while running, 60s otherwise; on `running → done` it auto-invalidates `["factor-per-stock"]` + portfolio factor queries. The existing manual **Rebuild cache** button is preserved (still grids-only). Concurrent startup-run vs 5pm Scheduled Task run in a separate process is tolerated — last-writer-wins per-row upserts. **New tests** [tests/analysis/precompute-freshness.test.ts](tests/analysis/precompute-freshness.test.ts) (11 cases). **`npm test` 280 · `npx tsc --noEmit` clean.** **Factors HORIZON presets + auto-precompute at market close (2026-06-13):** The free-form Window dropdown is replaced by a single **HORIZON** segmented control with 4 presets fixing the regression window — Short-Term 90d (`63`), Standard 365d (`252`, default), Long-Term 2y (`504`), Very Long-Term 3y (`756`) — in [FactorToolbar.tsx](src/components/analysis/factors/shared/FactorToolbar.tsx). **Attribution Period** is decoupled and offers trailing `1D / 5D / 1M / 3M / 6M / 1Y` (default `1Y`) under every horizon; 1D/5D are count-based, 1M/3M/6M/1Y are calendar offsets ([period.ts](src/lib/factors/attribution/period.ts), [period-log.ts](src/lib/factors/attribution/period-log.ts), [PeriodSelect.tsx](src/components/analysis/factors/shared/PeriodSelect.tsx), [AttributionClient.tsx](src/components/analysis/attribution/AttributionClient.tsx)). A shorter attribution period than the window is intentional and valid (stable betas fit on a long sample, applied to a recent slice). Store: `FactorWindow` = `63|252|504|756`, `FactorPeriod` = the 6 trailing labels, defaults `252`/`1Y`, persist **v11** migration reseeds legacy values; dead `WindowSelect.tsx` deleted. `GRID_CACHE_WINDOWS` = `[63, 252, 504, 756]` so the precompute cache covers exactly the 4 horizons (Very Long-Term 756 added; 21/42/126/378/1260 dropped). **Auto-precompute**: `tsx` added as a devDependency (the `job:*` scripts require it), [scripts/daily-precompute.ps1](scripts/daily-precompute.ps1) wraps `npm run job:daily` (repo-root CWD for Prisma `.env`, logs to `logs/`), and [scripts/register-daily-task.ps1](scripts/register-daily-task.ps1) registers Windows Scheduled Task **"MarketMap Daily Precompute"** (weekdays 17:00 local / 5pm ET, `-StartWhenAvailable`, battery-allowed). Task registered + verified; 4/4 grids pre-built (1209-1214 rows, asOf 2026-05-01 — refreshes to current on the next `job:daily` run). **`npm test` 269 · `npx tsc --noEmit` clean.** **Factors tab daily precompute cache + UX trim (2026-06-13):** Per-Stock Factors view is now served from a precomputed cache for instant loads. New `PerStockGridSnapshot` Prisma model (`@@unique([model, regressionWindow])`) holds the full-universe grid as JSON; [factor-per-stock-cache.service.ts](src/server/services/factor-per-stock-cache.service.ts) (`read/writePerStockGridCache`, `precomputeAllPerStockGrids` over MACRO14 × the 8 UI `WINDOW_PRESETS` 21/42/63/126/252/378/504/1260). [per-stock/route.ts](src/app/api/analysis/factors/per-stock/route.ts) is cache-first with live-compute write-through (sector/sub-theme filters bypass cache, filtered client-side). New `POST /api/analysis/factors/per-stock/precompute` route; daily orchestrator [scripts/daily-precompute.ts](scripts/daily-precompute.ts) (`npm run job:daily`: price-tail → factor pipeline → grid precompute) + grid-only [scripts/precompute-per-stock-grid.ts](scripts/precompute-per-stock-grid.ts) (`npm run job:precompute-grid`). Per-Stock toolbar shows "Cached as of {date}" + "Rebuild cache". **MACRO14-only**: `MODEL_PRESET_NAMES = ["MACRO14"]`, Model dropdown removed from `FactorToolbar` (FF presets kept for back-compat/API only). **Attribution periods** trimmed globally to `YTD / 1.5Y / 2Y / 3Y` (`FactorPeriod`, `PeriodSelect`, `period.ts`, `period-log.ts`, store v10 migration). **Attribution client** now resolves non-200 → `null`, fetches for the whole portfolio view, and shows a clear insufficient-history message instead of "unavailable yet". **Controls rollup**: Window + Attribution Period side-by-side with a shared `InfoTooltip`. **Factor hover popups**: new `howCalculated` on every `FactorDef` + reusable [FactorTooltip](src/components/analysis/factors/shared/FactorTooltip.tsx) on the return-waterfall labels. Note: `npx prisma generate` still hits the benign `query_engine-windows.dll.node` EPERM when `npm run dev` holds the lock — `db push` and the generated `.d.ts`/JS still update correctly. **`npm test` 269** (was 165) **· `npx tsc --noEmit` clean.** **RF unit-mismatch fix (2026-04-26):** closed a long-standing convention bug in how the Risk-Free rate was read from `FactorReturnDaily`. Stored RF values have always been **daily simple decimal** (matching every other code in the table — KF's native CSV is `RF (%)` per day, divided by 100 at ingest), but five reader sites — [factor-engine.service.ts](src/server/services/factor-engine.service.ts) (lines 127, 355), [factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts) (line 299), [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) (line 314), and [factor-drivers.service.ts](src/server/services/factor-drivers.service.ts) (line 82) — divided by 252 again at read time, treating the stored daily decimal as if it were annual. Effective `r_f_daily ≈ 7e-7` instead of the correct ~1.7e-4, so every "excess return" calculation silently produced (almost exactly) the **total return**. Per-stock detail panels for INTC 1Y showed `Cumulative excess (geom.) = +311%` which was actually the total return; Google quotes the same INTC 1Y total at ~+302.58 %; the residual ~9 pp was the trading-252 vs calendar-365 start-date offset. Diagnosis via two new read-only scripts: [scripts/factor-intc-reconcile.ts](scripts/factor-intc-reconcile.ts) mirrors the timeseries service's math end-to-end and reports headline / total / RF compounded; [scripts/factor-rf-units-check.ts](scripts/factor-rf-units-check.ts) prints raw `FactorReturnDaily.value` rows for RF and a comparison factor (MOM) so the actual stored magnitude (~2e-4) is visible and unambiguous. **Fix (Option A — reader-side)**: dropped the `/ 252` divisor at all 5 reader call sites; updated [factor-pipeline-macro.service.ts](src/server/services/factor-pipeline-macro.service.ts) `dailyRf` extraction to consume the stored value directly (with a safe `0.045 / 252` fallback when no RF rows exist); updated the FRED DGS1MO back-fill in [factor-pipeline.service.ts](src/server/services/factor-pipeline.service.ts) to convert FRED's native annual decimal to daily *before* calibration, so the calibration shift is now a small additive 1-3 bp correction operating on commensurate units instead of the previous accidental ~−0.044 cross-unit hack that happened to land at the right magnitude. The FRED **fetcher** (`fetchDgs1moRfDaily`) keeps returning FRED-native annual decimal — that's the right contract for a data provider; the pipeline service is responsible for the unit conversion before persisting. **Test contract update** in [tests/analysis/factor-rf-fred.test.ts](tests/analysis/factor-rf-fred.test.ts): the previous "RF readers stay on the /252 convention" describe block was inverted — now pins the daily-decimal contract and a synthetic 252-day end-to-end identity `(1 + Σ_log(1 + r_excess)) * (1 + Σ_log(1 + r_f)) ≡ (1 + Σ_log(1 + r_total))` to 1e-12. Existing fetcher / calibration tests unchanged (algorithm is unit-blind). **Docstring sync**: corrected RF unit comments in [factor-codes.ts](src/lib/factors/definitions/factor-codes.ts), [factor-pipeline.service.ts](src/server/services/factor-pipeline.service.ts) header, [factor-engine.service.ts](src/server/services/factor-engine.service.ts) RF block comment, [fred.provider.ts](src/infrastructure/providers/fred.provider.ts) (clarified fetcher returns annual decimal — it's the consumer's job to convert before storing). **Post-fix data step**: re-ran `scripts/factor-pipeline-refresh.ts` once to overwrite all `EQ / RATES / COMM / FX / SHORT_VOL / TREND` rows in `factor-pipeline-macro.service.ts` (which compose `ETF_total_return − dailyRf`) with the corrected excess-of-RF semantics across the Yahoo history span; the spread factors `LOCAL_EQ / EM / INFL / CROWD / BAB / QMJ` are RF-neutral and unaffected. **Verification**: post-fix INTC 252d 60d-rolling reconcile — RF cum ~+5.16 % (was +0.01 %); `Total ≈ +301 %` (unchanged, depends only on stock prices); headline excess geom. ~+281 % ≈ `(1 + total) / (1 + rf) − 1`; identity holds to 1e-12. UI: per-stock detail panel for INTC 1Y now shows headline ~+281 %, `Total ≈ +301 %`, reconciles within ~1 pp of Google's +302.58 %. **Out of scope**: switching the headline from excess to total (excess remains the OLS LHS and the right primary metric for a factor-attribution panel — the user-visible discrepancy with Google was caused by the bug, not by the metric choice); migrating individual `RF` rows in the DB (already correct under the new contract); annualizing RF storage (rejected — would create a one-off exception in a table where every other code is daily simple). **`npm test` 165** (was 164 — net +1: added the daily-decimal contract + end-to-end identity test, replaced the inverted "/252 reader contract" test) **· `npx tsc --noEmit` clean.** **INTC 1Y reconciliation gap closed (235 → 252 visible obs, 2026-04-26):** the prior FRED DGS1MO RF gap-fill cleared the staleness banner but a 1Y INTC view still landed at 235 visible obs against the requested 252, with the geometric headline `exp(Σ y_log) − 1 = +231%` falling ~70 pp short of the broker / Google "1Y return" of +302%. Diagnosis via new read-only [scripts/factor-window-coverage.ts](scripts/factor-window-coverage.ts) (mirrors `factor-freshness-check`'s style — enumerates trailing N candidate days, classifies each as `STOCK_MISSING` / `FACTOR_MISSING:<code>` / `WEEKEND_PHANTOM` / `BOTH MISSING` / `RF_ONLY_MISSING` / `KEPT`, and prints a verdict that branches on stock-side vs factor-side dominance) revealed two independent root causes. **Cause 1 — `tradingDatesAfter` timezone bug** in [factor-pipeline.service.ts](src/server/services/factor-pipeline.service.ts): the FRED RF back-fill walker mixed local-time `getDay()` with UTC `toISOString().slice(0, 10)`, which on any timezone west of UTC pushes Friday's date forward into Saturday and silently skips the corresponding Monday. Result: 2 phantom Saturday RF rows (2026-02-28, 2026-03-07) plus a missing Monday (2026-03-02). **Cause 2 — brittle `dPrev` chain** in [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) strict-drop loop: when `extendedWindowDates` (= `allDates.slice(-requiredHistory)` where `allDates` is the UNION of every loaded factor's publish dates) contains a phantom date the equity market was closed on (US holidays from AQR's BAB / QMJ global calendar; weekend / Good Friday RF rows from cause 1), the next real trading day's `dPrev` points at that phantom, `priceMap.get(dPrev)` returns undefined, and the day is silently dropped. Each phantom therefore eats exactly one real visible day. With ~17 US holidays + 3 weekend phantoms in a 392-day load × 1 victim each ≈ 20 lost days; minus the 60-day normalisation warm-up you land at `n ≈ 292`, and with rolling=60 `displayStartIndex = max(59, n − 252) = 59` → visible = 233 ≈ 235. ✓ Reconciles. **Fixes**: (1) rewrote `tradingDatesAfter` to use `getUTCDay()` + `setUTCDate()` exclusively + a UTC `today` reference so weekday filtering and ISO output speak the same calendar; (2) replaced the strict-drop loop's `if (dPrev != null) prev = priceMap.get(dPrev); else <backsearch>` with `if (dPrev != null) prev = priceMap.get(dPrev); if (prev === undefined) <backsearch>` so the existing 7-day backward search fires for both i=0 AND any later phantom dPrev — restores the policy without weakening it (still strict-drop on missing factor cells); (3) one-shot [scripts/factor-rf-phantom-cleanup.ts](scripts/factor-rf-phantom-cleanup.ts) deletes existing PROXY RF rows landing on Saturday/Sunday (target: 2 rows in this env); pipeline refresh then writes the missing real Monday(s). **UI surface**: per-stock detail + per-stock timeseries header strips now show `visible N / W d` (`Math.max(visibleObs, params.window)` denominator — clamps so the "fully populated" case never reads truncated as `252 / 252`) with an amber tint + extended hover when `N < W` pointing at the diagnostic script. **Total ≈ +X% headline sub-line** (in log mode only) renders directly under `Cumulative excess (geom.)`: server pre-computes `sumLogTotalVisible = Σ ln(1 + r_stock_i)` on the same `[displayStartIndex, n)` window the excess sum uses, exposes it on `PerStockLogTimeseries`, and `PerStockDetail` displays `exp(sumLogTotalVisible) − 1` so the user sees a number directly comparable to broker / Google "1Y return" (which quotes total, not excess) without a stateful UI toggle. Strict drop on `1 + r_stock ≤ 0` falls back to the excess sum so the line never renders an arithmetic-vs-geometric mismatch. **What this plan deliberately does NOT do**: does not relax the strict drop-row policy (silent zero-fill is what AGENTS.md explicitly bans), does not prune AQR's US-holiday rows from the DB (the dPrev-chain fix makes them harmless without re-ingesting), and does not add a "Total Return" mode toggle (the sub-line carries the same information without a stateful surface). **New tests** in [tests/analysis/factor-display-defaults.test.ts](tests/analysis/factor-display-defaults.test.ts): 4 cases — `Total ≈ exp(sumLogTotalVisible) − 1 ≡ Π(1 + r_stock) − 1` to 1e-12 over a 252-day synthetic chain, RF compounding ratio identity `(1 + total) / (1 + excess) − 1 ≡ Π(1 + rf) − 1`, denominator clamp at full-population (no `252 / 252` truncation), denominator surfaces requested window when `visibleObs < requestedWindow`. Post-fix INTC 1Y diagnostic: 17 dropped dates remain in the candidate-window UNION (all genuine US equity holidays where INTC didn't trade — correctly absent), 0 weekend phantoms, 0 RF-only gaps; the dPrev fix means none of them eat real visible days. Outcome: visible obs climbs from 235 to ~252, the `Total ≈` sub-line ties to the broker figure within RF-pp, and the geometric excess headline reconciles cleanly to `Total ≈ - Σ ln(1 + r_f)`. **`npm test` 164** (was 160 — +4 reconciliation/denominator) **· `npx tsc --noEmit` clean.** **FRED DGS1MO RF gap-fill (2026-04-26):** closes the "1 factor stale (RF)" warning by back-filling the RF column directly from FRED's keyless `fredgraph.csv` endpoint rather than waiting for Ken French's monthly publication. KF's RF lags the rest of the matrix by ~30-45 days because `factor-pipeline.service.ts` only synthesises ETF proxies for `MKT_RF / SMB / HML / MOM / RMW / CMA` over the FF gap — RF itself was never written for post-FF dates, so per-stock readers fell through to `rfByDate.get(d) ?? 0` and silently treated raw return as excess for the tail (~0.7 pp/yr drift at typical RF). **Approach**: `refreshFactorPipeline` now (1) walks Mon-Fri trading dates strictly after `lastFrenchDate`, (2) fetches **DGS1MO** (1-Month Treasury Constant Maturity, a daily smooth practitioner stand-in for KF's monthly bill — preferred over **DTB4WK** which adds auction-cycle noise around quarter-ends and stress days), (3) forward-fills FRED's bond-market holidays so every equity Mon-Fri date has a value, (4) calibrates the entire FRED series with a single mean-shift fitted on the trailing 63d KF/FRED overlap (level shift, not multiplicative — RF can be near-zero so ratio scaling is unstable; captures the small persistent 360 vs 365 day-count + Ibbotson-construction gap), and (5) upserts each as `RF` with `source: "PROXY"`. The same per-date map replaces the previous `latestRf` constant inside the SPY-RF proxy `MKT_RF` computation. **Units convention** (matches every other code in `FactorReturnDaily`): the stored value is the **daily simple decimal** of the risk-free asset (KF's native CSV is percent-per-day; the FRED DGS1MO back-fill is converted from annual to daily before calibration so it lands at the same magnitude). Readers `factor-engine`, `factor-per-stock`, `factor-per-stock-timeseries`, and `factor-drivers` consume the stored value directly with no per-read division. (NB: a previous version of this note erroneously claimed the stored value was annualized and required a /252 at read time — see the "RF unit-mismatch fix" entry below for details.) **No reader-side change required**, no env var (the existing `fetchFredSeries` keyless CSV path does not need `FRED_API_KEY`). **What this plan deliberately does NOT do**: does not fix the latent unit mismatch in the `proxy MKT_RF = SPY - rfDaily` line (the in-memory `rfDaily` map there stores annualized decimal that gets subtracted directly from a daily return — `normalizeProxyToFf` re-centres on the FF tail so the constant-offset semantics absorb the bug; addressing that is a separate decision). **New surfaces**: `fetchDgs1moRfDaily()` + reusable `fredForwardFill()` helper in [fred.provider.ts](src/infrastructure/providers/fred.provider.ts) and [factor-pipeline.service.ts](src/server/services/factor-pipeline.service.ts); pure-domain `calibrateRfShift()` in [factor-pipeline.ts](src/domain/calculations/factor-pipeline.ts); `rfRowsBackfilled` exposed on the `refreshFactorPipeline` return shape; `activeProxies.rf = "FRED:DGS1MO"` on the pipeline status when RF was sourced from FRED. **Failure mode**: a FRED outage logs an error and skips the back-fill rather than failing the whole pipeline; the existing amber `factorDataStale` banner continues to surface the missing RF tail with its pipeline-refresh remediation hint. **New tests** in [tests/analysis/factor-rf-fred.test.ts](tests/analysis/factor-rf-fred.test.ts): 8 cases — CSV parsing converts annualized percent to decimal, missing-day "." filtered, non-OK responses throw (no silent zero-fill), `calibrateRfShift` returns the additive shift matching KF's tail mean, no-op below `minOverlap`, FRED rows past `lastFfDate` excluded from calibration so post-FF outliers can't leak in, empty-input no-op, and the `0.044 / 252 ≈ 1.746e-4` reader contract pinned. **`npm test` 160** (was 152 — +8 RF gap-fill tests) **· `npx tsc --noEmit` clean.** Outcome: a fresh `POST /api/analysis/factors/pipeline-refresh` populates RF through the latest FRED close so `detectFactorStaleness` no longer flags RF; per-stock excess returns are now correct on the post-KF tail to within the calibrated ~1-3 bp residual instead of being inflated by the full RF level. **Factor data freshness guard (2026-04-26):** closed the final INTC reconciliation gap. After the burn-in budget fix the front end was wired correctly but a 1Y INTC view still landed at ~229 visible obs and a +211% headline against Google's price-only +302%. Diagnosis: the strict drop-row policy in [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) was silently capping the back of the visible window because the KF / AQR ETF-proxy splice rows in [factor-pipeline.service.ts](src/server/services/factor-pipeline.service.ts) and [factor-pipeline-macro.service.ts](src/server/services/factor-pipeline-macro.service.ts) hadn't been refreshed in this environment — the splice machinery (`normalizeProxyToFf` / `detectGap` / `buildFactorSeries` in [src/domain/calculations/factor-pipeline.ts](src/domain/calculations/factor-pipeline.ts)) exists and works, the rows just weren't in the DB. **Step 1 — pipeline refresh** (no code change): one `POST /api/analysis/factors/pipeline-refresh` call backfilled all 14 MACRO14 factor rows through the latest Yahoo close (verified via [scripts/factor-freshness-check.ts](scripts/factor-freshness-check.ts) — every MACRO14 code now reports `trading_day_lag = 0`). RF (Kenneth-French risk-free rate) is the only remaining stale series at ~40 trading days behind because there is no proxy splice for RF; the per-stock services already default `rfByDate.get(d) ?? 0` for missing entries so RF staleness doesn't drop rows but does silently treat raw return as excess for those days (~0.7 pp impact at typical RF). **Step 2 — server-side freshness guard**: new pure helper [src/lib/factors/diagnostics/freshness.ts](src/lib/factors/diagnostics/freshness.ts) exposes `detectFactorStaleness(factorByDate, usableFactors, { thresholdTradingDays = 3, rfByDate })` which computes the freshest day across the loaded factor matrix + RF (so weekend / holiday / timezone drift never produces false positives) and returns one `FactorStalenessEntry` per factor whose `lagTradingDays > threshold`. Reference-date semantics are deliberately relative (factors compared against each other, not a wall clock). RF folded in as a special non-`usableFactors` member because its zero-fallback silently inflates excess. Wired into both per-stock services — [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) attaches the result as `factorDataStale: FactorStalenessEntry[]` on `PerStockTimeseriesResult`; [factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts) does the same on `PerStockResult` so the snapshot detail panel sees the warning even before the timeseries fetch resolves. New `FactorStalenessEntry` type in [src/types/factors.ts](src/types/factors.ts). **Step 3 — UI banner**: extended the existing amber warning block in [PerStockDetail.tsx](src/components/analysis/factors/panels/PerStockDetail.tsx) (lines ~559-635 post-extension) with a fourth condition (`staleCount > 0`) that renders a `"N factor(s) stale (HML +1 - run /api/analysis/factors/pipeline-refresh)"` chip and folds a per-factor list (`HML last published 2026-02-27 (lags 40d)`) into the tooltip with a remediation hint pointing at the refresh endpoint. Banner is purely additive — stays hidden when nothing is wrong. **What this plan deliberately does NOT do**: does not relax the strict drop-row policy (silent zero-fill is bad practice; the splice rows being present is the correct fix), and does not ship a "decomposable window" date-range badge (rejected as redundant with the per-stock visible-obs strip already on the chart header). **New tests** in [tests/analysis/factor-staleness.test.ts](tests/analysis/factor-staleness.test.ts): 18 cases — `tradingDayDiff` semantics including the empirical 40-day KF lag, threshold off-by-one (lag exactly threshold ⇒ no flag, threshold+1 ⇒ flag), custom-threshold respect, null / NaN treatment, multi-factor sort (lag desc, factor asc on tie), missing-factor empty-set, RF folded into reference + flagged separately, RF + factor combined sort. **`npm test` 152** (was 134 — +18 staleness) **· `npx tsc --noEmit` clean.** Outcome: post-refresh INTC 1Y view shows 241 visible obs ending 2026-04-24 (residual 11-day gap from 252 is INTC price-coverage, separate from factor freshness), headline reconciles to within ~RF-excess pp of Google's price-only total. Future regressions where the proxy pipeline silently lapses surface as the amber `factorDataStale` chip with explicit remediation hint instead of a quiet back-of-window truncation. **Per-stock burn-in budget — normalisation warm-up (2026-04-26):** the per-stock detail panel was reporting a sub-window observation count (e.g. "168 obs (post burn-in)" for a 1Y view) instead of the full 252 visible days. Root cause was two stacked warm-ups in [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts): the existing extended-history loader budgeted `params.window + rollingWindow + DATA_BUFFER` to absorb the rolling-OLS burn-in, but `normalizeFactorRows` (called with `rollingWindow: 252, minObservations: 60`) silently strips the first ~60 days off the front of the loaded slice (no trailing window to vol-scale against). With the OLS warm-up budgeted but the normalisation warm-up unbudgeted, the surviving post-normalisation slice was only ~272 days, `displayStartIndex` collapsed onto the burn-in cutoff, and ~40-90 visible days got re-eaten. **Fix**: added `NORM_WARMUP = 60` constant (must match the `minObservations` value passed to `normalizeFactorRows` — change one, change the other) and folded it into the budget — `requiredHistory = params.window + requestedRollingHint + NORM_WARMUP + DATA_BUFFER`. Default 1Y/60d config: loaded slice grows 332 → 392 days; Match-snapshot 1Y: 524 → 584; both well below `MAX_HISTORY = 5040`. The per-stock detail header now reports ~252 obs (post burn-in) on a 1Y view of a long-history ticker, the burn-in `ReferenceArea` / `ReferenceLine` (Q2 lock) sit entirely before the visible window and stop rendering inside the chart (chart already gates on `burnInIndex > displayStartIndex`), and the `exp(Σ y_log) − 1` headline reconciles cleanly to the realised compounded return over the full visible window — closing the remaining INTC discrepancy that the log-attribution-as-default change had partially fixed. **Portfolio attribution / engine / risk decomposition unchanged** — they don't load extended history for a rolling-fit chart and have no rolling burn-in concept. **New tests** in [tests/analysis/factor-per-stock-display-window.test.ts](tests/analysis/factor-per-stock-display-window.test.ts): one asserts the new budget produces full visible coverage (`visible obs == params.window`, `burnInIndex < displayStartIndex`); one regression case proves that without `NORM_WARMUP` the visible region collapses by ~rolling-window days so future refactors can't silently drop the budget. **`npm test` 134** (was 132 — +2 display-window cases) **· `npx tsc --noEmit` clean.** **Log attribution as default (display-layer only, 2026-04-26):** Path B (log-return attribution) is now the default surface across per-stock detail/timeseries and the portfolio `AttributionClient`. The dual-pipeline backend, normalisation, and attribution services are unchanged — only the display layer flipped. Persisted store default `factorAttributionMode` moved from `"simple"` to `"log"` with a `version: 2` persist migration so existing browsers reseed. Per-stock waterfall headline now displays the compounded geometric `exp(Σ y_log) − 1` (e.g. INTC 1Y now reads +302% instead of arithmetic +123%) via a new `headlineOverride` prop on the shared `Waterfall` component (bars stay log-percent and still sum to the inner Σ y_log; the override decouples display value from the bar-span calculation). The previous "Σ y_log = …% · exp(.) − 1 = …%" footer was compressed to a one-line `Σ log contribs = X% → exp(Σ) − 1 = Y% ✓` reconciliation strip (✓ green when daily identity gap < 1e-6). The Simple/Log mode toggle was removed from both `PerStockTimeSeries` and `AttributionClient`; in log mode the chart's heavy realised line plots the geometric path `exp(cumLog) − 1` so its right edge ties to the panel headline, and a thin dashed `__cumLog` reference line shows where the factor / α / ε stack closes by daily identity. The portfolio period attribution table gains a leading `Total Excess` column with geometric primary + `Σ log = …%` muted sub-line. New shared inline `<LogModeMethodology />` badge ([LogModeMethodology.tsx](src/components/analysis/factors/panels/LogModeMethodology.tsx)) lives next to the headline on both surfaces and discloses the demoted arithmetic Σ y_simple and identity-gap on hover. Strict-drop fallback (any 1+r ≤ 0 in the visible window) silently degrades to Path A and surfaces an amber banner above the waterfall explaining that the displayed headline is NOT a compounded total. The log-vs-simple branching policy is single-sourced through a new pure helper [pickHeadlineValue](src/lib/factors/attribution/headline-picker.ts) consumed by `PerStockDetail`'s memo, with [tests/analysis/factor-display-defaults.test.ts](tests/analysis/factor-display-defaults.test.ts) pinning the contract (geometric matches compounded daily chain to 1e-12; null/non-finite logSum triggers fallback + banner flag). **`npm test` 132** (was 125 — +7 display-defaults tests) **· `npx tsc --noEmit` clean.** **Log-return dual-mode attribution (full stack, 2026-04-26):** added a parallel **Path B** log-return regression and attribution that coexists with the existing simple-return Path A so multi-period attribution reconciles to compounded realised excess. Motivation: per-stock waterfalls sum `Σ y_simple_excess` over the visible window, which is a different quantity than a 1Y compounded total return — the two cannot tie. Path B transforms the LHS to `y_log = ln(1 + r_stock) − ln(1 + r_f)` and the design matrix to `x_log = ln(1 + f_simple)` (raw, NOT vol-scaled — Path A's normalisation is intentionally a stability tool kept off Path B), runs a second `rollingMultivariateOls` in log space, and reconciles the cumulative log sum to compounded geometric realised via `exp(Σ y_log) − 1`. New helpers in [src/lib/factors/attribution/log-returns.ts](src/lib/factors/attribution/log-returns.ts) (`logOnePlus`, `stockExcessLog`, `factorRowLog`, `expSumMinus1`) carry a strict-drop policy: any `1 + r ≤ 0` returns `null` and the entire log path is suppressed for that result so Path A is never affected. Server: [factor-engine.service.ts](src/server/services/factor-engine.service.ts) and [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) emit parallel log series and rolling fits (`portExcessLogReturns` / `factorLogReturns` / `rfLogReturns` / `endFitLog` / `rollingFitsLog` on `FactorEngineResult`; `log: PerStockLogTimeseries | null` on `PerStockTimeseriesResult`). [attribution.service.ts](src/server/services/attribution.service.ts) emits parallel `dailyLog` / `cumulativeLog` / `periodsLog` via three new lib modules ([daily-log.ts](src/lib/factors/attribution/daily-log.ts), [cumulative-log.ts](src/lib/factors/attribution/cumulative-log.ts), [period-log.ts](src/lib/factors/attribution/period-log.ts)). UI: [analysis store](src/store/analysis.ts) gained a persisted `factorAttributionMode: "simple" | "log"` (`setFactorAttributionMode`); per-stock detail and timeseries panels ([PerStockDetail.tsx](src/components/analysis/factors/panels/PerStockDetail.tsx), [PerStockTimeSeries.tsx](src/components/analysis/factors/panels/PerStockTimeSeries.tsx)) gained a Simple/Log toggle in the chart header that conditionally swaps cumulative chart data, waterfall identities, tooltip / legend labels, and adds a prominent `exp(Σ y_log) − 1 = X%` headline plus a non-additivity footnote; portfolio [AttributionClient.tsx](src/components/analysis/attribution/AttributionClient.tsx) gained the same toggle and footnote. **Risk decomposition (Euler) intentionally remains on Path A** in this pass — Path B is a return-attribution identity, not a variance model. New tests in [tests/analysis/factor-attribution-log-identity.test.ts](tests/analysis/factor-attribution-log-identity.test.ts) pin: log additive identity post burn-in, `exp(Σ y_log) − 1 == Π(1 + r_simple) − 1` to 1e-12, strict drop on `1 + r ≤ 0`, and `stockExcessLog` algebraic correctness. **Factor data normalization (2026-04-26):** all factor rows fed to OLS are now rolling vol-scaled (252-day trailing ending t−1, min 60 obs, ±5σ winsorized, 10% target annualized vol) at [src/lib/factors/regression/normalization.ts](src/lib/factors/regression/normalization.ts), applied at engine + per-stock snapshot + per-stock timeseries layers. **Attribution still uses raw factor returns** — both layers preserve `XRawKept` / `finalFactorRowsRaw` separately so cumulative β·r contributions remain in original economic units. `FactorDef` gained an `inputType` enum (`RETURN | FIRST_DIFFERENCE | AMBIGUOUS`); diagnostics flow through `FactorEngineResult.normalization`, `PerStockResult.normalization`, `PerStockTimeseriesResult.normalization`; `/api/analysis/factors/exposure` exposes `normalizationApplied: true` + the diagnostics block. **`npm test` 125** (was 117 — +4 normalisation, +4 log-identity) **· `npx tsc --noEmit` clean.** **Per-stock chart full-window rolling fits + grid Σα/Σε columns + waterfall residual relabel (2026-04-26):** rolling charts now display a complete set of valid rolling observations across the full chosen display window (e.g. Display W = 252 + Rolling W = 252 → ~252 valid rolling β / risk / return points instead of a thin right-edge cluster). Implemented in [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) via two-stage alignment: stage 1 calls `computeFactorCoverage` at `params.window` to lock `usableFactors` (preserves Q1 snapshot tie-out); stage 2 loads `requiredHistory = min(MAX_HISTORY=5040, params.window + rollingWindow + DATA_BUFFER=20)` aligned trading days using those locked factors. New `burnInIndex` field exposed on `PerStockTimeseriesResult`; `displayStartIndex` repurposed to `max(burnInIndex, n - params.window)` (the first VISIBLE chart day). Chart in [PerStockTimeSeries.tsx](src/components/analysis/factors/panels/PerStockTimeSeries.tsx) resets cumulative sums at `displayStartIndex` so the return stack starts at zero on the first visible day, slices risk/beta data identically, and gates the burn-in `ReferenceArea`+`ReferenceLine` to only render when `burnInIndex > displayStartIndex` (the rare windowFallback path); header strip surfaces `visible Xd`. Waterfall identity sums in [PerStockDetail.tsx](src/components/analysis/factors/panels/PerStockDetail.tsx) automatically follow because they iterate from `tsData.displayStartIndex`. **Per-stock grid summary columns**: [PerStockGrid.tsx](src/components/analysis/factors/panels/PerStockGrid.tsx) gained four sticky-after-Sector columns — `Alpha` and `Unexplained` (signed Bloomberg heatmap), `Realised σ` and `R²` (plain monospaced; R² muted < 30 %); divider 2px border separates summary columns from the factor heatmap. `Alpha` / `Unexplained` are populated by a fixed-60d rolling OLS run per ticker inside [factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts) (new `rollingAlphaPostBurnSum` / `rollingResidualPostBurnSum` / `rollingObservationsPostBurn` on `PerStockRow`; `gridRollingWindow` constant on `PerStockResult`). Cost: ~1-2s extra on a ~400-ticker grid load. Tooltips disclose the snapshot-vs-rolling caveat — per-factor return columns still use snapshot β × Σr_t so row sums are NOT expected to tie to realised return; `Alpha` / `Unexplained` will tie to the per-stock waterfall when chart's rolling W = 60d (default). **Label**: "Σ residual ε_t" → "Unexplained Residual" in waterfall + chart tooltip + chart legend. **New test**: [tests/analysis/factor-per-stock-display-window.test.ts](tests/analysis/factor-per-stock-display-window.test.ts) covers `burnInIndex` / `displayStartIndex` formula + extended-history snapshot tie-out + visible-obs invariant. **`npm test` (117)** + **`npx tsc --noEmit`** clean. **SHORT_VOL repointed to SVXY (2026-04-26):** the legacy `^PUT` (CBOE PutWrite Index) pull from Yahoo never returned historical bars (Yahoo's v8 chart endpoint refuses CBOE-licensed indices, same class as `^DXY`), so SHORT_VOL was silently dropped from `usableFactors` in every per-stock regression. Swapped to **SVXY** (ProShares Short VIX Short-Term Futures ETF) in [factor-pipeline-macro.service.ts](src/server/services/factor-pipeline-macro.service.ts) — one-line ticker swap inside the existing Yahoo worker pool. **Caveat documented in factor metadata**: ProShares cut SVXY's leverage from −1.0x to −0.5x effective 2018-02-27 after the XIV blow-up; pre/post-2018 series are structurally different short-vol exposures. Inception 2011-10-04 (~14yr history). Run `POST /api/analysis/factors/pipeline-refresh` to backfill; pre-existing zero `^PUT` rows can be cleared via `DELETE FROM "FactorReturnDaily" WHERE "factorCode" = 'SHORT_VOL'` if desired. **Per-stock timeseries window contract fix (2026-04-26):** `PerStockResult` now carries `regressionWindow: params.window` distinct from `windowUsed: alignedWindowDates.length` (count of aligned trading days). The `PerStockDetail` panel switched to `data.regressionWindow` so the rolling-window length passed to `/api/analysis/factors/per-stock/timeseries` matches what the snapshot was built with. **Per-stock timeseries adaptive-window fallback (2026-04-26):** the contract fix above unblocked the call but exposed a deeper asymmetry — the snapshot service runs OLS on `aligned.length` (silently accepts `n < params.window` when KF/RF data lags Yahoo by ~40 trading days, which is the common case under MACRO14), while the timeseries service hard-required `n >= effectiveWindow = max(params.window, minObs)`. Result: `/api/analysis/factors/per-stock/timeseries` returned 422 for ~every ticker × model that touched HML/MOM/SMB whenever the FF gap was non-zero. Fixed by relaxing the rolling window: when `n < requestedRolling + MIN_ROLLING_FITS - 1` (where `MIN_ROLLING_FITS = 30`), `effectiveWindow` is shrunk to `max(minObs, n - MIN_ROLLING_FITS + 1)` so the chart still gets ≥ 30 rolling fits. New `windowFallback: { requestedWindow, effectiveWindow, availableObservations, reason }` field on `PerStockTimeseriesResult` reports fallback only when meaningful. **Bloomberg-style rolling-window decoupling (2026-04-26):** per-stock timeseries now accepts independent `rollingWindow` (30/60/90/252/Match snapshot) separate from display/snapshot `window`; `PerStockTimeSeries` header shows both (`rolling W` + `display W`), and default rolling window is 60d for interpretable 5y charts. Selecting **Match** preserves snapshot tie-out semantics. **Dev tooling:** run one `next dev` at a time; `npm run dev` caps Node heap at 4GB (`cross-env`) and now defaults to **Turbopack** (`--turbopack`), with `npm run dev:webpack` as the webpack fallback; `onDemandEntries` keeps the last 8 visited routes compiled for 5min so tab switches don't recompile; Performance market-map 30s auto-refresh pauses while the tab is hidden; OneDrive often contends with `.next` webpack packs (`EPERM`/`EBUSY`, occasional missing chunks) — prefer a non-synced clone for heavy dev; `npx prisma generate` may `EPERM` on `query_engine-windows.dll.node`, but the .d.ts types and DB are still updated correctly (the .dll lock is benign for build/test); `rm -rf .next` is occasionally needed before `npx next build` after large changes. **Bloomberg-style analysis chrome** (maroon/amber tokens in `analysis.css` — no `@tailwind base` there so Preflight does not strip global body styles; Roboto Condensed + IBM Plex Mono; two-strip `TopBar` with horizontal module tabs only, no duplicate sidebar; `bbTooltipStyle`; signed heat ramps in `heatmap.ts` — diverging red ↔ neutral gray ↔ green for all heatmaps and the correlation matrix). **`npm test` (117)** + **`npx tsc --noEmit`** clean. **Factor diligence Phase 3 (2026-04-25):** locks in the rolling-vs-snapshot policy from the 15-question memo. **Q1 — rolling Euler window = snapshot window** in [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) (`effectiveWindow = params.window`, latest valid rolling Euler tie-out matches snapshot Euler share / total σ to floating-point precision, see [tests/analysis/factor-reconcile-snapshot.test.ts](tests/analysis/factor-reconcile-snapshot.test.ts)). **Q2 — burn-in explicit**: `displayStartIndex = effectiveWindow − 1` plumbed through; `PerStockTimeSeries` greys the burn-in via `ReferenceArea` + dashed `ReferenceLine` at `t = W`; all identity sums skip burn-in. **Q3 — no silent degradation**: [invertWithRidge](src/lib/factors/regression/matrix.ts) returns `failed: boolean` flagging genuine OLS failures (singular even after Tikhonov ridge); `RegressionFit.failed` propagated end-to-end; timeseries exposes `rollingFitFailures` + `rollingFitFailureDates[]`; days with `null` fits are skipped from cumulative sums (NOT zeroed); banner warns in `PerStockDetail`. **Strict drop-row**: missing factor returns drop the entire date and append to `droppedDates`; legacy zero-fill counters retained but always 0. **Q4 — var-gap badge**: `|gap| < 2%` no badge, `2-5%` neutral, `≥ 5%` amber; integrated into per-stock detail and `PortfolioTotalsPanel`. **Q5 — alpha disambiguation**: `Static alpha (ann.)` headline, `Σ rolling α_t` waterfall slot, `Σ residual ε_t` residual; identity `Σy = Σ(β·r) + Σα + Σε` printed under waterfall with reconciliation gap; amber sub-line on alpha when `|Σα|/|Σy| > 0.50`. **Q7 — multicollinearity flag-only** (no rolling-OLS regularization): per-stock VIF + κ on detail panel, amber ≥ 5 / red ≥ 10. **Q10 — scatter relabel**: card title `Rolling-Prediction Fit`, overlay shows BOTH `Rolling R²` and `Static R² (in-sample)`, plus slope-dilution caption. **Q13 — UX hierarchy**: 1-large + 3-small headline (Realised σ primary; Model σ + var-gap secondary; R² + Euler share paired diagnostic; Static α tertiary); reconciliation strip above waterfalls. **Q8 — portfolio risk reconciliation**: `FactorExposureSnapshot.realizedAnnualizedVol` + `varGapPct` populated by [/api/analysis/factors/exposure](src/app/api/analysis/factors/exposure/route.ts); `PortfolioTotalsPanel` "Total Risk" anchored to realised σ. **Misc**: `factorScenarioRunBody` Zod default flipped to `MACRO14`, window range expanded to `[20, 2520]`. **New diagnostic scripts**: [scripts/factor-reconcile.ts](scripts/factor-reconcile.ts) (per-ticker × per-window identity dump; CI exits 1 on tolerance breach), [scripts/factor-diligence-sweep.ts](scripts/factor-diligence-sweep.ts) (universe-wide health report). **Five new pure-math tests**: `factor-rolling-risk-identity`, `factor-attribution-identity`, `factor-rolling-failure`, `factor-strict-droprow`, `factor-reconcile-snapshot`. **Factor diligence Phase 2 (2026-04-25):** per-stock Σ recomputed on the regression-aligned date sample (Euler share now ties to OLS information set); legacy full-window Σ kept as `systematicShareEulerFullWindow` for reconciliation. Detail panel shows BOTH `R² (in-sample)` and `Variance share (Euler)` always-labelled. Total-vol headline anchored to **realised σ** (sample √Var(y) × √252) per Phase 2 lock-in; model-implied √(βΣβ + σ²_idio) reported as reconciliation with `varGapPct`. Grid `returnContribution` switched to additive `β × Σr_t` so it ties to the rolling-additive series in the time-series chart by construction (legacy geometric kept on each cell as `returnContributionGeometric`). Per-row zero-fill audit (`zeroFillCount`/`zeroFillRowCount` + aggregate `zeroFillAudit`) badged in the detail panel header. `alphaWindowSum`/`residualWindowSum` exposed with explicit "intercept ≠ skill" tooltip. Negative PCR cells carry a `topCovariers` explainer pointing at the dominant covariance partners. New multicollinearity panel under the correlation matrix (per-factor VIF + condition number κ via power iteration; pairs |ρ| ≥ 0.7 listed with severity colouring) — `src/lib/factors/market/multicollinearity.ts`, `FactorMarketContext.multicollinearity`. Correlation subtitle now states "matches regression window used for risk decomposition Σ". Predicted-vs-actual scatter has a slope < 1 interpretation tooltip (rolling-OLS shrinkage in low-DOF samples). Annualisation audit table in [decomposition.ts](src/lib/factors/risk/decomposition.ts) header (Σ ×252, idio daily ×252, α ×252, σ √252, R² none); per-factor excess-vs-raw audit table in [factor-codes.ts](src/lib/factors/definitions/factor-codes.ts) header. Beta-sensitivity sweep at [scripts/factor-sensitivity.ts](scripts/factor-sensitivity.ts) — `npx tsx scripts/factor-sensitivity.ts SPY,QQQ 30,60,90,252`.

 **Factor tab (MACRO14 overhaul, 2026-04-25):** `/factors` now ships with a top-level **Portfolio | Per-stock toggle**. New `MACRO14` model (default) replaces the academic-only presets as the default factor set: 7 macro asset-class factors (`EQ`, `LOCAL_EQ`, `RATES`, `COMM`, `EM`, `FX`, `INFL`) + 7 style risk premia (`SHORT_VOL`, `TREND`, `BAB`, `MOM`, `QMJ`, `HML`, `CROWD`); legacy CAPM/FF3/FF5/Carhart4/Extended presets still available in the model dropdown. Window presets switched to **30/60/90/180-day, 365-day, 5-year + custom range** in trading days; full 14-factor model auto-flags low-DOF windows. **Per-stock view** (`GET /api/analysis/factors/per-stock`) runs the regression engine per active universe constituent (only `Security.isActive=true`) and returns a stock × factor grid of β / return contribution / risk contribution; rendered as a Bloomberg-style heatmap with a `Beta | Return | Risk` metric toggle, sector/sub-theme filter pills, and a sticky right-side detail panel showing the selected stock's full breakdown + diagnostics (R², total vol, systematic share, alpha). New per-stock service in [src/server/services/factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts); per-window factor coverage check at [src/lib/factors/regression/coverage.ts](src/lib/factors/regression/coverage.ts) (drops factors whose inception is after the window start with status `INSUFFICIENT_HISTORY` so the UI can badge them — relevant for short-history series like DBMF (2019) and GVIP (2016)). **Data pipeline:** macro/style series ingested by new [factor-pipeline-macro.service.ts](src/server/services/factor-pipeline-macro.service.ts) — Yahoo refresh covers `ACWI, IEF, DBC, EEM, UUP, TIP, SPY, USMV, QUAL, DBMF, GVIP, SVXY` (worker pool, ~150ms politeness gap); BAB and QMJ pulled directly from AQR's free daily XLSX (~30MB; SheetJS OOMs so we parse via JSZip + targeted regex in [aqr-xlsx-parser.ts](src/infrastructure/providers/aqr-xlsx-parser.ts)); `USMV-SPY` and `QUAL-SPY` proxies splice the recent ~2-month AQR publish gap via the existing `normalizeProxyToFf` machinery; `FactorReturnDaily` reuses the `FF` source enum value for AQR rows. New deps: `xlsx` (SheetJS, optional — JSZip path is the actual hot path). `POST /api/analysis/factors/pipeline-refresh` now runs FF + macro pipelines in parallel via `Promise.allSettled` and returns per-pipeline status. Schema migration: `FactorCode` enum extended with the 14 new codes (`db push` clean). **Institutional features still present:** joint multivariate OLS, rolling betas, risk decomposition, attribution, drivers, scenarios, market context, drift alerts. Single-universe screener: Market Map ("Performance") dashboard with top-bar **Manage Tickers** modal (CSV Import / Paste Tickers / Current N) replacing the removed `/universe` page; **Current tab now supports per-row removal** via `DELETE /api/universes/:id/constituents?ticker=…` (UniverseConstituent row deleted, Security row preserved); `GET /api/universe/default` auto-resolves the one active universe; CSV/TSV parser requires 4 columns (Ticker, Name, Sector, Sub-Theme) with optional headers; **`POST /api/universes/:id/constituents` is now batched + timeout-safe for large (~1k row) pastes** — service de-dupes input by uppercased ticker, pre-loads existing securities outside the transaction, then runs a single short transaction (`deleteMany` → `createMany` security → `updateMany` reactivate → small per-row name updates → `findMany` IDs → `createMany` constituents) with `{ timeout: 30_000, maxWait: 10_000 }`; route sets `maxDuration = 120` and returns a JSON error body on failure; modal surfaces a count-aware parse-error message and colours the footer red/green based on outcome; **market map renders as a block-grouped Sector → Sub-Theme → Ticker hierarchy** — columns are Sector | Sub-Theme | Ticker | horizons, sectors are the default top-level rows styled as uppercase amber block headers, expanding a sector reveals its sub-themes as a contiguous "card" underneath (continuous left-edge spine via `box-shadow: inset 4px 0 0 …` on the sector column, internal row dividers suppressed, distinct row backgrounds `#1c2638` / `#0e1624` / `#091018`, `#06090f` spacer rows separating sectors); each level has its own visual rhythm — sector rows are tall + uppercase amber (`#f0b65d`, 700, wide letter-spacing), sub-theme rows are mid-density with cool light text (`#d8e3f0`, 500), ticker rows are compact monospaced slate (`#9eb0c8`); heat cells follow the same 3-step density (0.86 → 0.84 → 0.8 rem); per-row chevrons drill into sub-themes / tickers and the controls-strip **Show sub-themes / Hide sub-themes** button toggles only sector-level expansion; the client makes one `rowLevel=COMPANY` fetch and aggregates sector / sub-theme cells in-browser (avg of leaf metrics per horizon, matching the server) with **per-level heatmap ranges**; **dashboard auto-updates** — fires `?onlyMissing=true` price ingest on first load + after Apply, and re-fetches the market map every 30 s. Yahoo ingest is hardened against throttling: chart endpoint retries on HTTP 401/429/5xx with exponential backoff, universe ingest uses a small concurrent worker pool (3 workers, ~150 ms inter-request delay) and `maxDuration = 600` so a 400-ticker initial run completes; per-ticker failures don't abort the batch (returned in `failed[]`). Symbol normalisation is precise: single trailing letter only (`BRK.B` → `BRK-B`), foreign exchange suffixes preserved (`MC.PA`, `NOVO-B.CO`), bare US index codes auto-`^`-prefixed (`VIX` → `^VIX`), and explicit overrides for tickers whose Yahoo chart symbol differs (`DXY` → `DX-Y.NYB`, `ABB` → `ABBNY` post-NYSE-delisting). Portfolios + analytics unchanged; Yahoo chart/quote HTTP (no `yahoo-finance2`); APIs under `/api/*`. Commands: `npm install` → `npx prisma db push` (Postgres + `DATABASE_URL`) → `npm run dev` → `npm test` / `npm run build`.

## Project Scope

**MarketMap** is a web-based equity analytics platform for **market map visualization** and **portfolio construction / analytics**, built as a serious internal investment analytics product.

- **Core purpose:** Map performance and risk across a single user-defined **universe of stocks** using a fixed hierarchy (Sector → Sub-Theme → Company), and support multi-portfolio construction with analytics and benchmark comparison.
- **Primary problem:** Centralize universe management, historical prices, trading-day-based analytics, rankings, and portfolio metrics in one maintainable system with clear domain boundaries—no analytics logic in UI components.
- **Key capabilities:** One persistent ticker universe managed via a **Manage Tickers** modal on the Performance page (CSV Import with drop-zone, Paste Tickers, Current list with per-column search); ingestion requires 4 columns per row (Ticker, Name, Sector, Sub-Theme), CSV/TSV both accepted with optional headers; 10+ years of adjusted daily prices stored in PostgreSQL; trading-day return/volatility/Sharpe/excess return; matrix heatmap market map (all horizons as columns) rendered as a **block-grouped Sector → Sub-Theme → Ticker hierarchy** with per-row chevrons, a "Show sub-themes / Hide sub-themes" global toggle, and visual block grouping that ties each sector's sub-themes (and tickers) into one card-like cluster with clear gaps between sectors; sector/sub-theme/company rollups by average; portfolio builder (ticker + weight); portfolio vs S&P 500, NASDAQ, DOW; factor exposure module (proxies in stage 1); scheduled refresh and analytics jobs.
- **Expected architecture:** Modular monolith: UI → API → services → data (Prisma/PostgreSQL) → external market data providers behind interfaces. Financial math in `src/domain/calculations` and related services.

---

## Mirrored user rules (terminal / agent visibility)

The following mirrors the repository’s Cursor user rules so terminal agents and non-Cursor tools see the same guidance. **Do not treat this as a second source of truth**—when Cursor rules change, update this section to match.

### Follow all instructions

- Think about **all** instructions in user rules, user queries, skills, system reminders, and MCP tool descriptions in full. Do not skip or partially apply them.
- When a skill, rule, or tool specifies format, output structure, naming, or workflow, **follow it**.
- Pay special attention to constraints in tool/skill/MCP descriptions—they are requirements.
- When a skill is relevant, **read and follow** the skill file; do not only mention it.

### Real environment

- This is a real environment with shell and network—**run commands and investigate**; do not only tell the user what to run.
- Do not give up after one failure; try alternatives or diagnose and retry.
- **Date authority:** The `Today's date` field in the session is authoritative (e.g. 2026).
- If about to write instructions instead of executing, **execute** them.

### Communication (user-facing)

- Use code citations as ` ```startLine:endLine:path ` for existing code; opening fence on its own line; skip large chunks with `...` where needed.
- In non-citation code blocks, full commands—no `...` omissions.
- Prefer markdown links for web URLs; full paths/URLs, not shortened.
- Prose: precise, well-structured, complete sentences; proportional length; avoid filler; use **bold** and backticks sparingly; no `§` in user-facing text; no engagement-bait closings.

### Code and scope discipline

- Only change code required for the task; no drive-by refactors or unrelated files.
- Do not add markdown files the user did not ask for (exception: `PROJECT_BRIEF.md` / this file / scope process as per project rules).
- Read surrounding code; match style; reuse abstractions; avoid unnecessary comments and defensive try/catch.
- For UI: polished, consistent with existing design patterns.

### Reason about conversation history

- Latest messages inherit context; identify underlying goals and implicit requirements; refinements default to steering the current task, not canceling it.

### Cursor skills

When relevant, read skills from paths listed in the session (e.g. under `.cursor/skills` or `.codex/skills`) and follow them.

### PROJECT_BRIEF.md (living project brief)

- Keep **`PROJECT_BRIEF.md`** in the project root updated for scope, structure, and major decisions.
- **Before major work:** Check `PROJECT_BRIEF.md` exists; create using the project’s suggested structure if missing.
- **Update** when architecture, major components, scope, stack, or capabilities change materially. If uncertain, ask the user.

### Project scope in AGENTS.md

- Confirm a **Project Scope** section exists (this document).
- If missing, ask the user for a concise outline, create `AGENTS.md`, add **Project Scope** with their description, and keep it updated on material direction changes (ask the user if needed).

### AGENTS.md synchronization

- **AGENTS.md** should mirror **existing** user-defined Cursor rules (visibility only): **no new rules, no removed rules, no changed meaning**—reorganize for agent consumption.
- On Cursor rules updates, update `AGENTS.md` to stay aligned.
- **Status freshness check:** verify blockers with commands and update date stamps (see top of this file).

### Engineering principles (architecture, data, quality, security, observability, schema, discipline)

- **Modular monolith** with strict layers: `ui → api → services → data → database/external`. Lower layers do not depend on higher layers. Business logic framework-agnostic in services/domain. Data access centralized; no SQL/ORM in business rules beyond repositories.
- **Data:** Treat external inputs as untrusted; validate at boundaries; canonical internal model; parse/normalize external data; adapters for new source formats.
- **Quality:** Simple, readable code; explicit names; reasonable file/function size; no duplicated logic; tests for critical domain logic.
- **Reliability:** Testable design; domain invariants explicit; idempotent/ordered/retry behavior explicit; deliberate error handling (validation, user-facing, retryable, fault).
- **Security:** AuthZ at server boundaries; never rely on UI for security.
- **Observability:** Log errors and important events; no silent failures.
- **Schema/contracts:** No breaking public contract changes without impact note; safe migrations; avoid destructive DB changes unless approved.
- **Discipline:** No speculative abstraction; default to simplest correct design; consider simplify/reuse/remove before adding code.

---

## Technology (this repo)

- **Frontend:** Next.js (App Router) + React + TypeScript  
- **Backend:** Next.js Route Handlers + shared domain package layout under `src/`  
- **Database:** PostgreSQL  
- **ORM:** Prisma  
- **Jobs:** `tsx` scripts + scheduler-friendly modules (BullMQ/Redis optional later)  

---

## Commands (typical)

```bash
pnpm install
pnpm db:generate
pnpm db:migrate
pnpm dev
pnpm test
pnpm job:refresh   # when implemented; placeholder until jobs wired
```

Set `DATABASE_URL` in `.env` before Prisma operations.
