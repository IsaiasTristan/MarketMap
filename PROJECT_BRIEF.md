### Current Development Focus

Ship and harden the **equity market map and portfolio analytics** platform: single-universe screener with a top-bar **Manage Tickers** modal (CSV Import / Paste Tickers / Current) for ingestion; Yahoo HTTP price/benchmark ingest to Postgres; market-map matrix API + heatmap UI rendered as an **expandable Sector → Sub-Theme → Ticker tree** (no row-level filter; per-row chevrons + Expand all / Collapse all toggle); portfolio weights + benchmark analytics; keep financial math in `src/domain` and services out of presentational components.

### Key Decisions / Constraints

- **Single universe:** App operates as a single "universe of stocks" screener. `GET /api/universe/default` returns or lazily creates the one active universe (DB schema remains multi-universe-capable; UI only exposes one).
- **Multi-user / single admin (Cloudflare Access):** Authenticated identity comes from Cloudflare Access (verified `Cf-Access-Jwt-Assertion`; direct local access is trusted as the admin). One **shared master database** holds the global universe, prices, factor data, and precomputed regression grids — computed once by the admin's instance on a timer; every other user only **reads** that shared data. Role is derived from the `ADMIN_EMAILS` allow-list. Only the admin can edit the global ticker universe, ingest prices/benchmarks, and change securities delist/live status. Normal users are auto-provisioned on first visit and own only their **own portfolios** (every portfolio + every analysis endpoint that takes a `portfolioId` is scoped to the current user). Requires env: `CF_ACCESS_TEAM_DOMAIN`, `CF_ACCESS_AUD`, `ADMIN_EMAILS`.
- **Hierarchy (only):** Sector → Sub-Theme → Company. Drives market map rows, drill-down, rankings, and aggregations (parent metric = **average of leaf company metrics** unless later specified otherwise).
- **Trading-day semantics:** 1/5/21/63/126/252 trading days for 1D, 5D, 1M, 3M, 6M, 1Y; not calendar days.
- **Returns:** From **adjusted** close; daily return = \(P_t/P_{t-1} - 1\). Volatility: annualized **realized** vol from daily returns, \(\sigma_{ann} = \text{stdev} \times \sqrt{252}\). Sharpe uses configurable risk-free; zero-vol edge case must not divide by zero.
- **Stack:** Next.js + TypeScript, Prisma + PostgreSQL, market data **behind interfaces**; default adapter uses Yahoo **public** chart + quote HTTP (no API key; unofficial—see `docs/ARCHITECTURE.md`). Benchmarks: S&P 500, NASDAQ, DOW stored in `BenchmarkPriceHistory`.
- **Quality:** No analytics logic in React components—call services/API from UI.

### Major Changes Log

- **2026-06-20 (origin performance fixes + Cloudflare-Access multi-user/admin model):** Two performance fixes plus a multi-user authorization layer, in response to tunnel timeouts and a request to share one master database across users with a single admin. **Fix 1 — non-blocking startup catch-up:** [precompute-runner.ts](src/server/services/precompute-runner.ts) `startPrecompute()` now spawns the daily precompute as a separate OS process (`npm run job:daily`, the same entry point as the 17:00 Scheduled Task) with stdout/stderr teed to a timestamped `logs/` file, instead of running the ~10-minute CPU-bound regression chain inline on the web server's event loop (which had been starving every request for minutes after boot). `RunnerState` gained a `pid`; `lastSummary` is now always null (the structured summary lives in the child's log). **Fix 2 — batched market-map price load:** [market-map.service.ts](src/server/services/market-map.service.ts) replaced the per-constituent `loadRecentPrices` loop (~1,220 sequential DB round-trips on the full universe) with a single `priceHistory.findMany` over all securityIds bounded to a ~600-day window, grouped in memory and sliced to the last 320 bars each. **Multi-user model:** new `User` model + `UserRole` enum + nullable `Portfolio.userId` ([schema.prisma](prisma/schema.prisma), `db push` + one-shot [scripts/backfill-portfolio-owners.ts](scripts/backfill-portfolio-owners.ts) assigning existing portfolios to the admin). Identity/authz in [auth.service.ts](src/server/services/auth.service.ts) (`getCurrentUser` verifies the Cf-Access JWT via `jose` against the team certs + optional AUD, upserts the User, syncs role from `ADMIN_EMAILS`, throttles `lastLoginAt` to ≤1 write/hr; `requireAdmin`, `assertPortfolioAccess`, `assertPositionAccess`); route helpers in [guards.ts](src/lib/api/guards.ts) / [auth-response.ts](src/lib/api/auth-response.ts). Every portfolio CRUD route and every analysis endpoint that takes a `portfolioId` (pnl, weights, positions, risk, correlation, concentration, performance, stress ×3, factors exposure/risk/attribution/drivers/alerts/exposures/history/scenarios/fundamentals, attribution trade-stats) is now scoped to the current user (403 on foreign/unknown id); admin-only mutations (universe constituents add/delete, universe + benchmark ingest, securities health delist/live, per-stock precompute, pipeline-refresh, full data refresh) return 403 for non-admins. New `GET /api/me` and admin-only `GET /api/admin/users`. **UI gating** (advisory only — server enforces): [useMe.ts](src/lib/api/useMe.ts) hook; the market-map page hides Manage Tickers / Refresh and skips its auto-ingest + live tail-refresh for non-admins; the Data tab hides the securities delist/live edit controls for non-admins and adds an admin-only Users panel (email · role · last login). Env added: `CF_ACCESS_TEAM_DOMAIN`, `CF_ACCESS_AUD`, `ADMIN_EMAILS` (defaults to the sole admin; degrades to the unverified Access email header with a warning until the team domain is set). `npm test` 312; `npx tsc --noEmit` clean. Verified at runtime: `/api/me`, `/api/admin/users`, per-user portfolio list, 403 on foreign portfolio, and a non-blocking startup catch-up that spawned a child process while the server served requests concurrently.

- **2026-06-14 (Factors tab: period-driven portfolio decomposition + Total Return grid column + global tooltips):** Three changes. **(1) Decomposition panels respond to the Attribution Period.** The portfolio Total Return Decomposition and Variance Decomposition panels previously didn't change when flexing 1D…1Y because `AttributionResult.daily` was the rolling-OLS fit tail (burn-in–gated), which collapsed to a single point when history ≤ the horizon window. Now `daily`/`dailyLog` are a full-length **static-beta** series — the horizon end-fit betas applied across all aligned dates (`computeStaticBetaDailyAttribution` / `computeStaticBetaDailyLogAttribution`), with alpha as the realized-minus-systematic plug — so period slices and the period-sliced variance always have data. The cumulative time-series chart keeps using the rolling series. **(2) Total Return column** added to the portfolio holdings grid (`PortfolioFactorGrid`) to match the per-stock screener: per-holding realized period return, TOTAL row = the portfolio's realized period total (ties to the Total Return Decomposition headline). **(3) Global Factors-tab tooltips**: every factor label and derived metric (Total Return, R², Vol, Alpha, Unexplained, Market Beta, Top Factor Tilt, Factor Concentration, Systematic Risk) now has a concise hover popup with definition, calculation, and a **Data used** provenance line, driven by a new `dataSource` field on each `FactorDef` and a `getMetricDef` registry. `npm test` 312; `tsc --noEmit` clean.

- **2026-06-13 (Factors tab: working Attribution Period + factor hover overviews + per-stock price chart):** Three user-facing fixes/features. **(1) Attribution Period now actually drives the panels.** New shared slicer `resolvePeriodSlice(dates, label, refDate?)` in [period.ts](src/lib/factors/attribution/period.ts) (1D/5D = trailing observation count, 1M..1Y = calendar offset) and a `pickPeriodSummary(attribution, period, mode)` picker in [pick-period-summary.ts](src/lib/factors/attribution/pick-period-summary.ts) that resolves the selected period + attribution mode to one normalized summary (geometric `exp(Σy_log)−1` headline + additive log bars in log mode, arithmetic in simple mode, falling back across modes). [PortfolioTotalsPanel.tsx](src/components/analysis/factors/panels/PortfolioTotalsPanel.tsx) and [HeaderSummary.tsx](src/components/analysis/factors/panels/HeaderSummary.tsx) consume the picker (log-aware `headlineOverride`; explicit "⚠ Attribution unavailable for {period}" degraded copy instead of a silently static snapshot). The Per-Stock toolbar now shows the period control (`showPeriod` on [PerStockView.tsx](src/components/analysis/factors/panels/PerStockView.tsx)). **Per-stock grid** follows the period without re-running 400-ticker regressions: [factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts) precomputes a `periodSlices` record per row (Return = β×Σr over the slice, Σα / Σε from the same rolling OLS restricted to the slice, both simple + log), and the [per-stock route](src/app/api/analysis/factors/per-stock/route.ts) overlays Return / Alpha / Unexplained for the requested `period` (betas / risk / R² / vol stay on the horizon window); cache write-through stays period-agnostic and self-heals when a cached grid predates `periodSlices`. The [PerStockDetail.tsx](src/components/analysis/factors/panels/PerStockDetail.tsx) waterfall slices its sums to the period (RF-inclusive Total≈ line suppressed for sub-windows since `sumLogTotalVisible` is whole-window). **(2) Factor hover overviews**: `FactorTooltip` gained a `concise` prop (first-sentence definition + full `howCalculated`) and is now wired on all 14-factor column headers across [PerStockGrid](src/components/analysis/factors/panels/PerStockGrid.tsx), [PortfolioFactorGrid](src/components/analysis/factors/panels/PortfolioFactorGrid.tsx), and [SectorFactorHeatmap](src/components/analysis/factors/panels/SectorFactorHeatmap.tsx) (replacing native `title`). **(3) Per-stock price chart**: new [StockPriceChart.tsx](src/components/analysis/factors/panels/StockPriceChart.tsx) embedded in the detail popup above the attribution waterfall — range toggles 1D/5D/1M/6M/YTD/1Y/5Y/MAX (default 1D), price + change header, and a click-drag "measure return" gesture showing the % between two points. Backed by a hybrid [price-series.service.ts](src/server/services/price-series.service.ts) + [/api/analysis/securities/price-series](src/app/api/analysis/securities/price-series/route.ts): live Yahoo intraday (1m/5m via new `fetchYahooIntraday` in [yahoo-chart-http.ts](src/infrastructure/providers/yahoo-chart-http.ts)) for 1D/5D, stored daily adjusted closes for longer ranges. **New tests** [tests/analysis/factor-period-slice.test.ts](tests/analysis/factor-period-slice.test.ts) (11 cases: slicer count/calendar semantics + picker mode routing/fallback). **`npm test` 291** (was 280; +11) **· `npx tsc --noEmit` clean.** Note: the per-stock grid's period overlay needs the precompute cache rebuilt once (existing cached grids predate `periodSlices`); the route self-heals on first period request, or run "Rebuild cache".

- **2026-06-13 (Factors tab: server-startup catch-up + freshness indicator):** Added a safety net so the saved per-stock regression grids are always current to the last completed trading close, even when the 17:00 Windows Scheduled Task was missed (PC off/asleep). On Next.js server boot, [src/instrumentation.ts](src/instrumentation.ts) fires-and-forgets [maybeRunStartupCatchUp](src/server/services/precompute-runner.ts), which consults a new pure freshness helper ([precompute-freshness.ts](src/lib/factors/diagnostics/precompute-freshness.ts) — `lastTradingClose(now)` = most recent weekday whose 17:00 local close has passed; `getPrecomputeFreshness(db)` = oldest `computedAt` across the 4 expected MACRO14×HORIZON `PerStockGridSnapshot` rows, with `stale` true if any expected row is missing or older than the last close) and only kicks off the full daily chain if the cache is actually stale. The full chain (price tail → factor pipeline refresh → per-stock grid precompute for all 4 horizons) was extracted into a new [factor-daily-precompute.service.ts](src/server/services/factor-daily-precompute.service.ts) consumed by both [scripts/daily-precompute.ts](scripts/daily-precompute.ts) (Scheduled Task) and the in-memory [precompute-runner.ts](src/server/services/precompute-runner.ts) (startup). The runner exposes `RunnerState` (`idle | running | done | error`, `startedAt`, `finishedAt`, `lastTrigger`, `lastSummary`); a new [GET /api/analysis/factors/precompute-status](src/app/api/analysis/factors/precompute-status/route.ts) returns `{ freshness, runner }`. The per-stock toolbar's existing cache controls in [PerStockView.tsx](src/components/analysis/factors/panels/PerStockView.tsx) now show a coloured dot (green = current to last close · amber = stale · pulsing accent = refreshing) with a "Last saved {time} · as of {date}" label and a "Refreshing… (startup catch-up | manual)" state while the runner is active; on transition from running → done the client invalidates `["factor-per-stock"]` and the portfolio factor queries so the grids reload automatically. The poll cadence is 5s while running, 60s while idle. The manual "Rebuild cache" button is preserved (still hits the original grids-only endpoint). **New tests** in [tests/analysis/precompute-freshness.test.ts](tests/analysis/precompute-freshness.test.ts) (11 cases): `lastTradingClose` Fri-after/Fri-before/Sat/Sun/Mon-before/Mon-after; `stale` empty-cache, all-present-fresh, missing-window-stale, oldest-computedAt-limits, latestComputedAt/oldestAsOfDate sourcing. **`npm test` 280** (was 269; +11 freshness) **· `npx tsc --noEmit` clean.**

- **2026-06-13 (Factors tab: HORIZON presets + auto-precompute at market close):** Replaced the free-form Window dropdown with a single **HORIZON** segmented control of 4 presets that each fix the regression (training) window: Short-Term = 90 day (63 td), Standard = 365 day (252 td, default), Long-Term = 2 year (504 td), Very Long-Term = 3 year (756 td) — in [FactorToolbar.tsx](src/components/analysis/factors/shared/FactorToolbar.tsx). The **Attribution Period** selector is now decoupled and offers trailing horizons `1D / 5D / 1M / 3M / 6M / 1Y` (default `1Y`) for every HORIZON; 1D/5D are count-based (last N daily obs), the rest are calendar offsets ([period.ts](src/lib/factors/attribution/period.ts), [period-log.ts](src/lib/factors/attribution/period-log.ts), [PeriodSelect.tsx](src/components/analysis/factors/shared/PeriodSelect.tsx), [AttributionClient.tsx](src/components/analysis/attribution/AttributionClient.tsx)). This intentionally allows a shorter attribution period than the window (stable betas fit on a long sample, applied to a recent slice) and removes the prior statistically-invalid free combinations. Store: `FactorWindow` narrowed to `63|252|504|756`, `FactorPeriod` to the 6 trailing labels, defaults `252` / `1Y`, persist **v11** migration reseeds legacy values; dead `WindowSelect.tsx` (custom-days selector) deleted. `GRID_CACHE_WINDOWS` set to `[63, 252, 504, 756]` so the precompute cache covers exactly the 4 horizons. **Auto-precompute at market close**: added `tsx` as a devDependency (the `job:*` scripts depend on it), a [scripts/daily-precompute.ps1](scripts/daily-precompute.ps1) wrapper (sets repo-root CWD so Prisma reads `.env`, logs to `logs/`), and [scripts/register-daily-task.ps1](scripts/register-daily-task.ps1) which registers a **Windows Scheduled Task "MarketMap Daily Precompute"** running weekdays 17:00 local (5pm ET) with `-StartWhenAvailable` + battery-allowed; it runs `npm run job:daily` (price tail → factor pipeline → grid precompute for all 4 windows). Task registered + verified (next run Mon; 4/4 grids pre-built, 1209-1214 rows each). **`npm test` 269** **· `npx tsc --noEmit` clean.**

- **2026-06-13 (Factors tab: daily precompute cache + UX trim):** Made the Per-Stock Factors view load instantly and tightened its controls. **Precompute cache**: new `PerStockGridSnapshot` Prisma model (`@@unique([model, regressionWindow])`) stores the full-universe per-stock regression grid as JSON; [factor-per-stock-cache.service.ts](src/server/services/factor-per-stock-cache.service.ts) exposes `read/writePerStockGridCache` + `precomputeAllPerStockGrids` (MACRO14 × 8 windows = the UI's `WINDOW_PRESETS` 21/42/63/126/252/378/504/1260). The Per-Stock GET route ([per-stock/route.ts](src/app/api/analysis/factors/per-stock/route.ts)) is now cache-first with a live-compute write-through fallback (sector/sub-theme filters bypass the cache and filter client-side). New `POST /api/analysis/factors/per-stock/precompute` rebuilds all grids; a daily pre-open orchestrator [scripts/daily-precompute.ts](scripts/daily-precompute.ts) (`npm run job:daily`) chains price-tail refresh → factor pipeline refresh → grid precompute, and [scripts/precompute-per-stock-grid.ts](scripts/precompute-per-stock-grid.ts) (`npm run job:precompute-grid`) rebuilds grids only. Per-Stock toolbar gained a "Cached as of {date}" indicator + "Rebuild cache" button. **MACRO14-only**: `MODEL_PRESET_NAMES` trimmed to `["MACRO14"]` and the Model dropdown removed from `FactorToolbar` (academic FF presets remain defined for back-compat + the API `model` param, but are not UI-selectable or precomputed). **Attribution periods** trimmed globally to `YTD / 1.5Y / 2Y / 3Y` (dropped 1D/5D/MTD/QTD/1M/3M/6M/1Y/ITD — too short for a 14-factor regression); updated `FactorPeriod`, `PeriodSelect`, `period.ts`, `period-log.ts`, and a store v10 persist migration. **Attribution fixes**: the client query now resolves non-200 to `null` (no bogus result), is fetched for the whole portfolio view (so the Total-Return panel + period selector always have data), and the "unavailable yet" copy was replaced with a clear insufficient-history message. **Controls rollup**: Window + Attribution Period now sit side-by-side in the toolbar with a shared `InfoTooltip` explaining the difference. **Factor hover popups**: added a `howCalculated` field to every `FactorDef` and a reusable [FactorTooltip](src/components/analysis/factors/shared/FactorTooltip.tsx) wired into the return waterfalls (per-stock + portfolio) so hovering a factor/residual label shows what it is + how it's computed. Also fixed a pre-existing unrelated tsc error at the holdings route boundary (`sector: null → undefined`). **`npm test` 269** (was 165 — +9 cache round-trip/enumeration, attribution period tests updated) **· `npx tsc --noEmit` clean.**

- **2026-04-26 (RF unit-mismatch fix — readers / FRED back-fill / docs):** Closed a long-standing convention bug in how the Risk-Free rate was read from `FactorReturnDaily`. Stored RF values have always been **daily simple decimal** (matching every other code in the table — KF's native CSV is `RF (%)` per day, divided by 100 at ingest), but five reader sites — [factor-engine.service.ts](src/server/services/factor-engine.service.ts), [factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts), [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts), and [factor-drivers.service.ts](src/server/services/factor-drivers.service.ts) — divided by 252 again at read time, treating the stored daily decimal as if it were annual. Effective `r_f_daily ≈ 7e-7` instead of the correct ~1.7e-4, so every "excess return" calculation silently produced (almost exactly) the **total return**. Per-stock detail panels for INTC 1Y showed `Cumulative excess (geom.) = +311%` which was actually the total return; Google quotes the same INTC 1Y total at ~+302.58 %. Diagnosis via two new read-only scripts: [scripts/factor-intc-reconcile.ts](scripts/factor-intc-reconcile.ts) mirrors the timeseries service's math end-to-end and reports headline / total / RF compounded; [scripts/factor-rf-units-check.ts](scripts/factor-rf-units-check.ts) prints raw `FactorReturnDaily.value` rows so the actual stored magnitude is unambiguous. **Fix (Option A — reader-side)**: dropped the `/ 252` divisor at all 5 reader call sites; updated [factor-pipeline-macro.service.ts](src/server/services/factor-pipeline-macro.service.ts) `dailyRf` extraction to consume the stored value directly (with a safe `0.045 / 252` fallback when no RF rows exist); updated the FRED DGS1MO back-fill in [factor-pipeline.service.ts](src/server/services/factor-pipeline.service.ts) to convert FRED's native annual decimal to daily *before* calibration, so the calibration shift is now a small additive 1-3 bp correction operating on commensurate units instead of the previous accidental ~−0.044 cross-unit hack that happened to land at the right magnitude. The FRED **fetcher** (`fetchDgs1moRfDaily`) keeps returning FRED-native annual decimal — that's the right contract for a data provider; the pipeline service is responsible for the unit conversion. **Test contract update** in [tests/analysis/factor-rf-fred.test.ts](tests/analysis/factor-rf-fred.test.ts): the previous "RF readers stay on the /252 convention" describe block was inverted — now pins the daily-decimal contract and a synthetic 252-day end-to-end identity `(1 + Σ_log(1 + r_excess)) * (1 + Σ_log(1 + r_f)) ≡ (1 + Σ_log(1 + r_total))` to 1e-12. Existing fetcher / calibration tests unchanged (algorithm is unit-blind). **Docstring sync**: corrected RF unit comments in [factor-codes.ts](src/lib/factors/definitions/factor-codes.ts), [factor-pipeline.service.ts](src/server/services/factor-pipeline.service.ts) header, [factor-engine.service.ts](src/server/services/factor-engine.service.ts) RF block comment, [fred.provider.ts](src/infrastructure/providers/fred.provider.ts) (clarified fetcher returns annual decimal — it's the consumer's job to convert before storing). **Post-fix data step**: re-ran `scripts/factor-pipeline-refresh.ts` once to overwrite all `EQ / RATES / COMM / FX / SHORT_VOL / TREND` rows in `factor-pipeline-macro.service.ts` (which compose `ETF_total_return − dailyRf`) with the corrected excess-of-RF semantics across the Yahoo history span; the spread factors `LOCAL_EQ / EM / INFL / CROWD / BAB / QMJ` are RF-neutral and unaffected. **Verification**: post-fix INTC 252d 60d-rolling reconcile — RF cum ~+5.16 % (was +0.01 %); `Total ≈ +301 %` (unchanged, depends only on stock prices); headline excess geom. ~+281 % ≈ `(1 + total) / (1 + rf) − 1`; identity holds to 1e-12. **Out of scope**: switching the headline from excess to total (excess remains the OLS LHS and the right primary metric for a factor-attribution panel — the user-visible discrepancy with Google was caused by the bug, not by the metric choice); migrating individual `RF` rows in the DB (already correct under the new contract); annualizing RF storage (rejected — would create a one-off exception in a table where every other code is daily simple). **`npm test` 165** (was 164 — net +1: added the daily-decimal contract + end-to-end identity test, replaced the inverted "/252 reader contract" test) **· `npx tsc --noEmit` clean.**

- **2026-04-26 (INTC 1Y reconciliation gap closed — phantom-row + dPrev-chain fixes):** Closed the headline reconciliation gap that the prior FRED DGS1MO back-fill exposed: an INTC 1Y view was still landing at 235 visible obs (vs the requested 252) and a +231% geometric headline against a broker / Google "1Y return" of +302%. New read-only diagnostic [scripts/factor-window-coverage.ts](scripts/factor-window-coverage.ts) classifies each candidate trading day in a window as `STOCK_MISSING` / `FACTOR_MISSING:<code>` / `WEEKEND_PHANTOM` / `BOTH MISSING` / `RF_ONLY_MISSING` / `KEPT` and emits a verdict for stock- vs factor-side dominance. The diagnostic surfaced two independent root causes. **Cause 1**: a timezone bug in `tradingDatesAfter` in [factor-pipeline.service.ts](src/server/services/factor-pipeline.service.ts) — the FRED RF back-fill walker mixed local-time `getDay()` with UTC `toISOString().slice(0, 10)`, which on any timezone west of UTC pushes Friday's date forward into Saturday and silently skips the corresponding Monday (produced 2 phantom Saturday RF rows + 1 missing real Monday). Fixed by walking dates with `getUTCDay()` + `setUTCDate()` and a UTC `today` reference exclusively. **Cause 2 (the dominant cause)**: a brittle `dPrev` chain in [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts)'s strict-drop loop. `extendedWindowDates = allDates.slice(-N)` where `allDates` is the UNION of every loaded factor's publish dates; that union can include phantom dates the equity market was closed on (US holidays from AQR's BAB/QMJ global calendar; weekend / Good Friday RF rows from cause 1). When `extendedWindowDates[i-1]` is a phantom, `priceMap.get(dPrev)` returns undefined and the next real trading day is silently dropped — i.e. each phantom eats exactly one real visible day. Fixed by extending the existing 7-day backward date-search (which previously only ran for `i=0`) to also fire whenever `priceMap.get(dPrev)` is undefined, so the chain always finds the closest real prior trading-day price. **One-shot cleanup** [scripts/factor-rf-phantom-cleanup.ts](scripts/factor-rf-phantom-cleanup.ts) deletes existing PROXY RF rows that landed on Sat/Sun (target: 2 rows in this env); a subsequent pipeline refresh writes the missing real Monday(s). **UI surface**: per-stock detail + per-stock timeseries header strips now show `visible N / W d` (`Math.max(visibleObs, params.window)` denominator — clamps so the fully-populated case never reads truncated as `252 / 252`) with an amber tint + extended hover when `N < W` pointing at the diagnostic script. **Total ≈ +X% headline sub-line** (in log mode only) renders directly under `Cumulative excess (geom.)`: server pre-computes `sumLogTotalVisible = Σ ln(1 + r_stock_i)` on the same `[displayStartIndex, n)` window the excess sum uses, exposes it on `PerStockLogTimeseries`, and `PerStockDetail` displays `exp(sumLogTotalVisible) − 1` so the user sees a number directly comparable to broker / Google "1Y return" (which quotes total, not excess) without a stateful UI toggle. Strict drop on `1 + r_stock ≤ 0` falls back to the excess sum so the line never renders an arithmetic-vs-geometric mismatch. **What this plan deliberately does NOT do**: it does not relax the strict drop-row policy (silent zero-fill is what AGENTS.md explicitly bans), does not prune AQR's US-holiday rows from the DB (the dPrev-chain fix makes them harmless without re-ingesting), and does not add a "Total Return" mode toggle (the sub-line carries the same information without a stateful surface). **New tests** ([tests/analysis/factor-display-defaults.test.ts](tests/analysis/factor-display-defaults.test.ts)): 4 cases — `Total ≈ exp(sumLogTotalVisible) − 1 ≡ Π(1 + r_stock) − 1` to 1e-12 over a 252-day synthetic chain, RF compounding ratio identity `(1 + total) / (1 + excess) − 1 ≡ Π(1 + rf) − 1`, denominator clamp at full population (no `252 / 252` truncation), denominator surfaces requested window when `visibleObs < requestedWindow`. Post-fix INTC 1Y diagnostic: 17 dropped dates remain in the candidate-window UNION (all genuine US equity holidays where INTC didn't trade — correctly absent from the visible window), 0 weekend phantoms, 0 RF-only gaps; the dPrev fix means none of them eat real visible days. Outcome: visible obs climbs from 235 to ~252, the `Total ≈` sub-line ties to the broker figure within RF-pp, and the geometric excess headline reconciles cleanly to `Total ≈ - Σ ln(1 + r_f)`. **`npm test` 164** (was 160 — +4 reconciliation/denominator) **· `npx tsc --noEmit` clean.**

- **2026-04-26 (FRED DGS1MO RF gap-fill):** Closes the persistent "1 factor stale (RF)" warning by back-filling the RF column directly from FRED's keyless [`fredgraph.csv`](https://fred.stlouisfed.org/docs/api/fred/usage.html) endpoint instead of waiting for Ken French's monthly RF publication. Ken French's RF lags the rest of the matrix by ~30-45 days because [`factor-pipeline.service.ts`](src/server/services/factor-pipeline.service.ts) only synthesises ETF proxies for `MKT_RF / SMB / HML / MOM / RMW / CMA` over the FF gap — RF itself was never written for post-FF dates, so per-stock readers fell through to `rfByDate.get(d) ?? 0` and silently treated raw return as excess for the tail (~0.7 pp/yr drift at typical RF). **Approach**: `refreshFactorPipeline` now (1) walks Mon-Fri trading dates strictly after `lastFrenchDate`, (2) fetches **DGS1MO** (1-Month Treasury Constant Maturity, the standard daily smooth practitioner stand-in for KF's monthly bill — preferred over **DTB4WK** which adds auction-cycle noise around quarter-ends and stress days), (3) forward-fills FRED's bond-market holidays so every equity Mon-Fri date has a value, (4) calibrates the FRED series to KF's level with a single mean-shift fitted on the trailing 63d KF/FRED overlap (level shift, not multiplicative — RF can be near-zero so ratio scaling is unstable; captures the small persistent 360 vs 365 day-count + Ibbotson-construction gap), and (5) upserts each as `RF` with `source: "PROXY"`. The same per-date map replaces the previous `latestRf` constant inside the SPY-RF proxy `MKT_RF` computation. **Units convention locked** (matches existing KF RF rows): `FactorReturnDaily.value` for `RF` = annualized rate as **decimal** (e.g. 0.044 for 4.4 %) — readers `factor-engine`, `factor-per-stock`, `factor-per-stock-timeseries`, and `factor-drivers` divide by 252 to get the daily rate. **No reader-side change required**, no env var needed (the existing `fetchFredSeries` keyless CSV path does not require `FRED_API_KEY`). **What this plan deliberately does NOT do**: does not fix the latent unit mismatch in the proxy `MKT_RF = SPY - rfDaily` line where `rfDaily` stores annualized decimal subtracted directly from a daily SPY return — `normalizeProxyToFf` re-centres on the FF tail and absorbs the constant offset; addressing that is a separate decision called out in the audit. **New surfaces**: `fetchDgs1moRfDaily()` + reusable `fredForwardFill()` helper; pure-domain `calibrateRfShift()` in [factor-pipeline.ts](src/domain/calculations/factor-pipeline.ts); `rfRowsBackfilled` exposed on the `refreshFactorPipeline` return shape; `activeProxies.rf = "FRED:DGS1MO"` on the pipeline status when RF was sourced from FRED. **Failure mode**: a FRED outage logs an error and skips the back-fill rather than failing the whole pipeline; the existing amber `factorDataStale` banner continues to surface the missing RF tail with its pipeline-refresh remediation hint. **New tests** ([tests/analysis/factor-rf-fred.test.ts](tests/analysis/factor-rf-fred.test.ts)): 8 cases — CSV parsing converts annualized percent to decimal, missing-day "." filtered, non-OK responses throw (no silent zero-fill), `calibrateRfShift` returns the additive shift matching KF's tail mean, no-op below `minOverlap`, FRED rows past `lastFfDate` excluded from calibration so post-FF outliers can't leak in, empty-input no-op, and the `0.044 / 252 ≈ 1.746e-4` reader contract pinned. **`npm test` 160** (was 152 — +8 RF gap-fill tests) **· `npx tsc --noEmit` clean.** Outcome: a fresh `POST /api/analysis/factors/pipeline-refresh` populates RF through the latest FRED close so `detectFactorStaleness` no longer flags RF; per-stock excess returns are now correct on the post-KF tail to within the calibrated ~1-3 bp residual instead of being inflated by the full RF level.

- **2026-04-26 (factor data freshness guard — back-end strict-drop diagnosis + UI banner):** Closed the final INTC reconciliation gap. After the burn-in budget fix the front-end was wired correctly but a 1Y INTC view still landed at ~229 visible obs and a +211% headline against Google's price-only +302%. Diagnosis: the `strict drop-row` policy in [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) was silently capping the back of the visible window because the Kenneth-French and AQR proxy splices in [factor-pipeline.service.ts](src/server/services/factor-pipeline.service.ts) and [factor-pipeline-macro.service.ts](src/server/services/factor-pipeline-macro.service.ts) hadn't been refreshed in this environment — the splice machinery exists and works (`normalizeProxyToFf` + `detectGap`) but the rows weren't in the DB, so HML / MOM / SMB / RMW / CMA / BAB / QMJ all stopped at the last KF / AQR publication date and every aligned date past that point was dropped. **Step 1 — pipeline refresh** (no code change): one `POST /api/analysis/factors/pipeline-refresh` call backfilled all 14 MACRO14 factor rows through the latest Yahoo close (verified via [scripts/factor-freshness-check.ts](scripts/factor-freshness-check.ts) — every MACRO14 code now reports `trading_day_lag = 0`). RF (Kenneth-French risk-free rate) is the only remaining stale series at ~40 trading days behind because there is no proxy splice for RF; the per-stock services already default `rfByDate.get(d) ?? 0` for missing entries so RF staleness no longer drops rows but does silently treat raw return as excess for those days, an acceptable trade-off given the headline impact (~0.7 pp at typical RF). **Step 2 — server-side freshness guard**: new pure helper [src/lib/factors/diagnostics/freshness.ts](src/lib/factors/diagnostics/freshness.ts) exposes `detectFactorStaleness(factorByDate, usableFactors, { thresholdTradingDays = 3, rfByDate })` which computes the freshest day across the loaded factor matrix + RF (so weekend / holiday / timezone drift never produces false positives) and returns one `FactorStalenessEntry` per factor whose `lagTradingDays > threshold`. Reference-date semantics are deliberately relative (factors compared against each other, not a wall clock) so a uniformly one-weekend-late panel produces zero entries. RF is folded in as a special non-`usableFactors` member because its zero-fallback silently inflates excess. Wired into both per-stock services — [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) attaches the result as `factorDataStale: FactorStalenessEntry[]` on `PerStockTimeseriesResult`; [factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts) does the same on `PerStockResult` so the snapshot detail panel sees the warning even before the timeseries fetch resolves. New `FactorStalenessEntry` type added to [src/types/factors.ts](src/types/factors.ts). **Step 3 — UI banner**: extended the existing amber warning block in [PerStockDetail.tsx](src/components/analysis/factors/panels/PerStockDetail.tsx) (lines 559-635 post-extension) with a fourth condition (`staleCount > 0`) that renders a `"N factor(s) stale (HML +1 - run /api/analysis/factors/pipeline-refresh)"` chip and folds a per-factor list (`HML last published 2026-02-27 (lags 40d)`) into the tooltip together with a remediation hint pointing at the refresh endpoint and a back-reference to this plan. Banner is purely additive — stays hidden when nothing is wrong. **New tests** ([tests/analysis/factor-staleness.test.ts](tests/analysis/factor-staleness.test.ts)): 18 cases — `tradingDayDiff` semantics including the empirical 40-day KF lag, threshold off-by-one (lag exactly threshold ⇒ no flag, threshold+1 ⇒ flag), custom-threshold respect, null / NaN treatment, multi-factor sort (lag desc, factor asc on tie), missing-factor empty-set, RF folded into reference + flagged separately, RF + factor combined sort, and the prod-state dual-stale case. **`npm test` 152** (was 134 — +18 staleness) **· `npx tsc --noEmit` clean.** **What this plan deliberately does NOT do**: it does not relax the strict drop-row policy (silent zero-fill is bad practice; the splice rows being present is the correct fix), and it does not ship a "decomposable window" date-range badge (rejected as redundant with the per-stock visible-obs strip already on the chart header). Outcome: post-refresh INTC 1Y view shows 241 visible obs ending 2026-04-24 (the residual 11-day gap from 252 is INTC price-coverage, not factor freshness — a separate concern outside this plan's scope), and the headline geometric total reconciles to within ~RF-excess pp of Google's price-only total. Future regressions where the proxy pipeline silently lapses will surface as the amber `factorDataStale` chip with an explicit remediation hint instead of a quiet back-of-window truncation.

- **2026-04-26 (per-stock burn-in budget — normalisation warm-up):** Closed a remaining INTC reconciliation gap surfaced by the log-attribution-as-default change. The per-stock detail panel was still reporting a sub-window count (e.g. "168 obs (post burn-in)" for a 1Y view) instead of the full 252-day visible window, even though the timeseries service already pre-loaded extended history (`requiredHistory = params.window + rollingWindow + DATA_BUFFER`) specifically to absorb the rolling-OLS warm-up. The cause was a **second warm-up stacked on top**: `normalizeFactorRows` (called with `rollingWindow: 252, minObservations: 60`) silently strips the first ~60 days off the front of the loaded slice (no trailing window to vol-scale against), and the existing slack didn't budget for it — so `displayStartIndex` collapsed onto the OLS burn-in cutoff and ate ~40 visible days. **Fix** ([src/server/services/factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts)): added a `NORM_WARMUP = 60` constant (kept in sync with the `minObservations` value passed to `normalizeFactorRows`) and folded it into the slack budget — `requiredHistory = params.window + requestedRollingHint + NORM_WARMUP + DATA_BUFFER`. For the default 1Y / 60d-rolling config the loaded slice grows from 332 → 392 trading days; for Match-snapshot 1Y from 524 → 584; both well below the existing `MAX_HISTORY = 5040` cap. **Downstream effects**: the per-stock detail header now reports ~252 obs (post burn-in) on a 1Y view of a long-history ticker, the burn-in `ReferenceArea` / `ReferenceLine` (Q2 lock) sit entirely before the visible window and stop rendering inside the chart (chart already gates on `burnInIndex > displayStartIndex`), and the `exp(Σ y_log) − 1` headline now reconciles cleanly to the realised compounded return over the full visible 1Y. **Portfolio attribution unchanged** — the engine and `attribution.service` run a single OLS over the requested window with no rolling-burn-in concept. **Risk decomposition (Path A)** unaffected. **Tests** ([tests/analysis/factor-per-stock-display-window.test.ts](tests/analysis/factor-per-stock-display-window.test.ts)): added two pure-math regression cases — one asserting the new budget produces a fully-populated visible region (visible obs == params.window, `burnInIndex < displayStartIndex`) and one regression case proving that without `NORM_WARMUP` the visible region collapses by ~rolling-window days (so future refactors can't silently drop the budget). **`npm test` 134** (was 132 — +2 display-window cases) **· `npx tsc --noEmit` clean.**

- **2026-04-26 (log attribution as default — display-layer only):** Promoted Path B (log-return attribution) from an opt-in toggle to the **default surface** across the per-stock detail panel, the per-stock time-series chart, and the portfolio `AttributionClient`. The motivation was a UX disconnect: even with the dual-pipeline backend shipping `tsData.log` and `attribution.cumulativeLog` as of the prior change, the persisted store default was `factorAttributionMode: "simple"` and the Simple/Log toggle was buried in the chart strip, so users (e.g. INTC over a 1Y window) saw an arithmetic Σ y_simple of +123% in the headline next to a chart whose realised line ended at +302% — exactly the reconciliation gap log mode was built to close. **Display-layer changes only — the dual-pipeline backend, normalisation, and attribution services are unchanged.** **Store** ([src/store/analysis.ts](src/store/analysis.ts)): default `factorAttributionMode` flipped to `"log"` and the persist `version` bumped to `2` with a one-time `migrate` that rewrites any persisted `"simple"` to `"log"` so existing local-storage entries don't override the new default. **PerStockDetail** ([src/components/analysis/factors/panels/PerStockDetail.tsx](src/components/analysis/factors/panels/PerStockDetail.tsx)): rule changed from "log only when toggle is set" to `useLog = tsData?.log != null`; the `Waterfall` headline is now `exp(Σ y_log) − 1` (the geometric reconciliation that ties to compounded realised) via a new `headlineOverride` prop on the shared component, while bars stay log-percent and continue to sum to the inner Σ y_log. The legacy "Σ y_log = X% · exp(.) − 1 = Y%" block was replaced with a single-line reconciliation strip `Σ log contribs = X% → exp(Σ) − 1 = Y% ✓` (✓ green when daily identity gap < 1e-6). When the log path strict-drops (any 1+r ≤ 0 in the visible window), an amber banner above the waterfall warns the headline is NOT a compounded total. **PerStockTimeSeries** ([src/components/analysis/factors/panels/PerStockTimeSeries.tsx](src/components/analysis/factors/panels/PerStockTimeSeries.tsx)): the `Mode: Simple | Log` pill was removed; in log mode the realised line `__actual` plots the GEOMETRIC compounded path `exp(Σ y_log) − 1` so its right edge ties to the panel headline, and a thin dashed `__cumLog` reference line shows where the factor / α / ε stack closes by daily identity. Tooltip and legend labels updated accordingly. **AttributionClient** ([src/components/analysis/attribution/AttributionClient.tsx](src/components/analysis/attribution/AttributionClient.tsx)): symmetric treatment — Mode pill replaced with an inline `Total Excess: +X%` headline (geometric) plus the methodology popover; period table gains a leading `Total Excess` column showing the geometric primary with `Σ log = …%` muted underneath in log mode. **New shared methodology popover** ([src/components/analysis/factors/panels/LogModeMethodology.tsx](src/components/analysis/factors/panels/LogModeMethodology.tsx)): hover-only `i` badge sits next to the per-stock and portfolio "Total Excess Return" headlines and documents the log identity, geometric conversion, the demoted arithmetic Σ y_simple, and the daily identity residual gap. **New pure helper** ([src/lib/factors/attribution/headline-picker.ts](src/lib/factors/attribution/headline-picker.ts)): `pickHeadlineValue({ arithmeticSum, logSum })` is the single source of truth for the log-vs-simple branching policy; consumed by `PerStockDetail`'s memo (replacing the inline `useLog ? expSumMinus1(innerSum) : simpleSum` ternary). The `Waterfall` component itself gained a small additive `headlineOverride?: { value; format? }` prop that decouples the displayed total from the value driving bar-span scaling — allows the bars to stay calibrated to log contributions while the headline shows the (much larger) geometric translation, without compressing every bar to half-width. **New tests** ([tests/analysis/factor-display-defaults.test.ts](tests/analysis/factor-display-defaults.test.ts)): pin the headline-picker contract — log path picked when `logSum` is finite (returns `exp(Σ) − 1` not arithmetic Σ), strict fallback to arithmetic + banner when `logSum` is null or non-finite, geometric matches a manually-compounded daily return chain to 1e-12, sign-honesty when geometric and arithmetic disagree, and integration shapes for both per-stock and attribution payloads. **`npm test` 132** (was 125 — +7 display-defaults tests) **· `npx tsc --noEmit` clean.**

- **2026-04-26 (log-return dual-mode attribution):** Added a parallel **Path B** log-return regression and attribution pipeline that coexists with the existing simple-return Path A. Motivation: the per-stock attribution waterfall sums `Σ y_simple_excess` over the visible window, which is mathematically distinct from a compounded total return — e.g. a 1Y total return of 302% is a `Π(1 + r_t) − 1` quantity, while the panel header showed an arithmetic sum like 123% over the same days. Path B closes the reconciliation loop by transforming the LHS to `y_log = ln(1 + r_stock) − ln(1 + r_f)` and the factor design matrix to `x_log = ln(1 + f_simple)` (raw, NOT vol-scaled — vol normalisation is intentionally a Path-A-only stability tool); a second `rollingMultivariateOls` runs in log space, and the daily identity `Σ y_log = Σ(β·x_log) + Σα + Σε` reconciles to the compounded geometric realised excess via `exp(Σ y_log) − 1`. New module [src/lib/factors/attribution/log-returns.ts](src/lib/factors/attribution/log-returns.ts) carries `logOnePlus`, `stockExcessLog`, `factorRowLog`, `expSumMinus1` with a strict-drop policy: any `1 + r ≤ 0` returns `null` and the entire log path is suppressed for that result so Path A is never affected. Server-side: [factor-engine.service.ts](src/server/services/factor-engine.service.ts) and [factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts) build parallel log series and emit new optional payload fields (`portExcessLogReturns`, `factorLogReturns`, `rfLogReturns`, `endFitLog`, `rollingFitsLog` on `FactorEngineResult`; `log: PerStockLogTimeseries | null` on `PerStockTimeseriesResult`); [attribution.service.ts](src/server/services/attribution.service.ts) emits parallel `dailyLog` / `cumulativeLog` / `periodsLog` via three new lib modules ([daily-log.ts](src/lib/factors/attribution/daily-log.ts), [cumulative-log.ts](src/lib/factors/attribution/cumulative-log.ts), [period-log.ts](src/lib/factors/attribution/period-log.ts)). UI: [analysis store](src/store/analysis.ts) gained a persisted `factorAttributionMode: "simple" | "log"` (`setFactorAttributionMode`); per-stock detail and timeseries panels ([PerStockDetail.tsx](src/components/analysis/factors/panels/PerStockDetail.tsx), [PerStockTimeSeries.tsx](src/components/analysis/factors/panels/PerStockTimeSeries.tsx)) gained a Simple/Log toggle in the chart header that conditionally swaps cumulative chart data, waterfall identities, tooltip and legend labels, and adds a prominent `exp(Σ y_log) − 1 = X%` headline plus a non-additivity footnote ("per-factor bars are additive in log space ONLY"); portfolio [AttributionClient.tsx](src/components/analysis/attribution/AttributionClient.tsx) gained the same toggle and footnote. **Risk decomposition (Euler)** intentionally remains on Path A in this pass — Path B is a return-attribution identity, not a variance model. New tests in [tests/analysis/factor-attribution-log-identity.test.ts](tests/analysis/factor-attribution-log-identity.test.ts) pin: log additive identity post burn-in, `exp(Σ y_log) − 1 == Π(1 + r_simple) − 1` to 1e-12, strict drop policy on `1 + r ≤ 0`, and `stockExcessLog` algebraic correctness. **`npm test` 125** (was 117 — +4 normalisation, +4 log-identity) **· `npx tsc --noEmit` clean**.

- **2026-04-26 (factor data normalization):** All factor rows fed to OLS are now rolling vol-scaled (252-day trailing, ending t−1, min 60 obs, ±5σ winsorized, target 10% annualized vol) and held against a strict-drop policy when normalisation cannot be computed. Implemented in new module [src/lib/factors/regression/normalization.ts](src/lib/factors/regression/normalization.ts); applied at the engine ([factor-engine.service.ts](src/server/services/factor-engine.service.ts)), per-stock snapshot ([factor-per-stock.service.ts](src/server/services/factor-per-stock.service.ts)), and per-stock timeseries ([factor-per-stock-timeseries.service.ts](src/server/services/factor-per-stock-timeseries.service.ts)) layers. **Crucially, attribution still uses raw factor returns** — both layers preserve `XRawKept` / `finalFactorRowsRaw` separately from the normalized inputs so cumulative β·r contributions remain in original economic units. Factor metadata gained an `inputType` enum (`RETURN | FIRST_DIFFERENCE | AMBIGUOUS`) on every `FactorDef`. New `FactorNormalizationDiagnostics` (config + ambiguous factors + insufficient-obs counts + total rows dropped) flows through `FactorEngineResult.normalization` and `PerStockResult.normalization` / `PerStockTimeseriesResult.normalization`; `/api/analysis/factors/exposure` adds `normalizationApplied: true` and exposes the diagnostics block. Tests in [tests/analysis/factor-normalization.test.ts](tests/analysis/factor-normalization.test.ts) cover trailing-window correctness, no-look-ahead invariant, min-obs gating, ambiguous-input flagging, and OLS coefficient sign stability between raw and normalized inputs.

- **2026-04-26 (rolling beta period decoupled from display window):** Per-stock time-series now follows Bloomberg-style semantics where rolling beta period is user-selectable and independent from the snapshot/display regression window. Added `rollingWindow` to `/api/analysis/factors/per-stock/timeseries` and `runPerStockTimeseries` params; chart defaults to 60d rolling even when display window is 5y, with selector options `30/60/90/252/Match`. `Match` keeps the old Q1 tie-out behavior. Added persistent Zustand state `factorTsRollingWindow` and passed it through `PerStockDetail` query keys/URL. Header now shows both windows (`rolling W` and `display W`) to avoid ambiguity. The previous amber fallback banner is now thresholded: shown only for significant shrink (`|gap| >= 5%`) or short effective series (`< 60` rolling points), while existing fit-failure/drop-row warnings remain unchanged.

- **2026-04-26 (SHORT_VOL + per-stock timeseries fixes):** Two bug fixes surfaced by the per-stock detail panel. **(1) SHORT_VOL repointed from `^PUT` to SVXY.** Yahoo's v8 chart endpoint refuses to serve historical bars for `^PUT` (CBOE S&P 500 PutWrite Index) — same class of CBOE-licensed indices as `^DXY` which we already had to override. Result was that `factorComposed.SHORT_VOL` was never written, the coverage check silently dropped SHORT_VOL from `usableFactors`, and every per-stock MACRO14 regression ran without the short-vol factor. Swapped to **SVXY** (ProShares Short VIX Short-Term Futures ETF) — one-line ticker change inside the existing Yahoo worker pool. SVXY inception is 2011-10-04, so the effective SHORT_VOL history is ~14 years; existing factor coverage check handles this gracefully (drops SHORT_VOL at long windows that pre-date its inception). **2018 leverage caveat documented in [factor-codes.ts](src/lib/factors/definitions/factor-codes.ts)**: ProShares cut SVXY's effective leverage from −1.0x to −0.5x on 2018-02-27 after the XIV blow-up — pre/post-2018 are structurally different short-vol exposures and a single regression β should be interpreted with that regime break in mind. To activate: hit `POST /api/analysis/factors/pipeline-refresh` once. **(2) Per-stock timeseries window contract fix + adaptive fallback.** Two layers. **Layer 1:** Phase 3 Q1 lock made the timeseries service require `n >= max(params.window, minObs)`, but `PerStockDetail` was passing `data.windowUsed` (= `alignedWindowDates.length`, often 5,000+ for MACRO14) as the rolling-window length. Result: the route always returned 422 and the panel showed "Could not load factor time series for this window — return waterfall unavailable." Added explicit `regressionWindow: params.window` field to `PerStockResult`; `PerStockDetail` now passes `data.regressionWindow` so the rolling window length matches the snapshot regression. `windowUsed` semantics unchanged (still drives the "N trading days" subtitle in `PerStockView`). **Layer 2:** Layer 1 exposed an asymmetry between the two services — the snapshot accepts `aligned.length < params.window` (runs OLS on whatever it has when KF/RF data lags Yahoo by ~40 trading days, which is the common case under MACRO14), while the timeseries hard-required `n >= effectiveWindow`. The route still 422'd for nearly every ticker. Relaxed the timeseries gate: when `n < params.window + 29`, `effectiveWindow` is shrunk to `max(minObs, n - 29)` so we always get ≥ 30 rolling fits. Q1 snapshot tie-out is intentionally relaxed in this case (the snapshot is also running on `n`, so the relaxation is consistent). New `windowFallback` field on `PerStockTimeseriesResult` reports the fallback (`{ requestedWindow, effectiveWindow, availableObservations, reason }`); `PerStockDetail` surfaces it as an amber banner. Verified across `POWL`, `SOXL`, `SQQQ`, `AAL`, `SPY`, `NVDA` — all return 200 with `windowFallback.reason = "INSUFFICIENT_HISTORY"` (KF lag of ~40 trading days as of 2026-02-27 cutoff). **`npm test` 113 pass · `npx tsc --noEmit` clean.**

- **2026-04-25 (factor diligence Phase 3):** Locked in the rolling-vs-snapshot diligence policy from the 15-question memo. **Q1 — Rolling Euler window now equals the snapshot window** in `factor-per-stock-timeseries.service.ts` (`effectiveWindow = params.window`); the latest valid rolling Euler tie-out matches the snapshot Euler share / total σ to floating-point precision (covered by `tests/analysis/factor-reconcile-snapshot.test.ts`). **Q2 — Burn-in handling is explicit**: `displayStartIndex = effectiveWindow − 1` is plumbed through to UI (`PerStockTimeSeries` greys the burn-in window via `ReferenceArea`, dashed `ReferenceLine` at `t = W`); all identity sums in waterfalls, scatter plots, and attribution periods skip burn-in. **Q3 — No silent degradation**: `invertWithRidge` now returns `failed: boolean` flagging genuine OLS failures (singular even after Tikhonov ridge); `RegressionFit.failed` is propagated end-to-end; `factor-per-stock-timeseries.service.ts` exposes `rollingFitFailures` + `rollingFitFailureDates[]`; days with `null` fits are skipped from cumulative sums (NOT zeroed); a banner warns in `PerStockDetail` when `> 0`. **Strict drop-row policy** implemented in both per-stock services (`factor-per-stock.service.ts` + timeseries): missing factor returns drop the entire date and append to `droppedDates`; legacy zero-fill counters retained but always 0. **Q4 — Var-gap badge thresholds**: `|varGapPct| < 2%` → no badge, `2-5%` → neutral, `≥ 5%` → amber; integrated into per-stock detail and `PortfolioTotalsPanel`. **Q5 — Alpha disambiguation**: `PerStockDetail` headline reads `Static alpha (ann.)`, the return waterfall α slot is `Σ rolling α_t`, residual is `Σ residual ε_t`; identity sub-line `Σy = Σ(β·r) + Σα + Σε` printed under the waterfall, with a numeric reconciliation gap; amber sub-line on the alpha cell when `|Σα|/|Σy| > 0.50`. **Q7 — Multicollinearity flag-only**: per-stock VIF + condition number κ surfaced via a footer card, amber ≥ 5 / red ≥ 10. **Q10 — Scatter relabel**: card title `Rolling-Prediction Fit`, overlay shows BOTH `Rolling R²` (derived from the scatter) and `Static R² (in-sample)` (from snapshot OLS), plus an explicit slope-dilution caption. **Q13 — UX hierarchy**: 1-large + 3-small headline (Realised σ primary; Model σ + var-gap secondary; R² + Euler share paired diagnostic; Static α tertiary); reconciliation strip above waterfalls. **Q8 — Portfolio risk reconciliation**: `FactorExposureSnapshot` now carries `realizedAnnualizedVol` + `varGapPct` populated by `/api/analysis/factors/exposure`; `PortfolioTotalsPanel` "Total Risk" headline anchored to realised σ for parity with per-stock. **Q9 misc**: `factorScenarioRunBody` Zod default flipped to `MACRO14`, window range expanded to `[20, 2520]`. **New diagnostic scripts**: `scripts/factor-reconcile.ts` (per-ticker × per-window identity dump; CI exits 1 on tolerance breach), `scripts/factor-diligence-sweep.ts` (universe-wide health report — drop-row impact, rolling-fit failures, var-gap distribution, multicollinearity, identity check). **Five new pure-math tests added** (`factor-rolling-risk-identity`, `factor-attribution-identity`, `factor-rolling-failure`, `factor-strict-droprow`, `factor-reconcile-snapshot`). **`npm test` 113 / 113 pass · `npx tsc --noEmit` clean.**

- **2026-04-25 (factor diligence Phase 2):** Locked in methodology decisions across the per-stock factor stack. **Σ recomputed per-stock on regression-aligned dates** in `factor-per-stock.service.ts` (Euler decomposition now ties to the OLS information set; legacy full-window Σ retained for reconciliation as `systematicShareEulerFullWindow`). **Variance-share dual reading**: detail-panel headline shows BOTH `R² (in-sample)` and `Variance share (Euler)` always labelled. **Total volatility headline anchored to realised σ** (sample √Var(y) × √252) per Phase 2 lock-in; model-implied √(βΣβ + σ²_idio) shown as reconciliation sub-line with `varGapPct`. **Grid return contribution switched to additive arithmetic** `β × Σr_t` to tie out with the rolling-additive series in the time-series chart by construction; legacy geometric `β × (Π(1+r) − 1)` kept on each cell as `returnContributionGeometric` for transparency. **Zero-fill audit instrumented**: per-row `zeroFillCount`/`zeroFillRowCount` + aggregate `zeroFillAudit` in the per-stock service; UI badges any stock with imputed cells. **Σα and Σε exposed** on every per-stock row (`alphaWindowSum`, `residualWindowSum`) with explicit "intercept ≠ skill" tooltip. **Negative PCR explainer**: `topCovariers` per-cell shows the dominant covariance partners driving any negative variance share; surfaced in cell title + risk waterfall sub-line. **Multicollinearity panel** added to the Correlations view: per-factor VIF (= diag(R⁻¹)) + condition number κ via power iteration on the correlation matrix and its inverse; pairs |ρ| ≥ 0.7 listed with amber/red severity colouring (`src/lib/factors/market/multicollinearity.ts`, `FactorMarketContext.multicollinearity`). **Cov vs corr windows aligned + labelled** ("matches regression window used for risk decomposition Σ" in the correlations subtitle). **Predicted-vs-actual scatter** carries an explicit `slope < 1` interpretation tooltip (rolling-OLS shrinkage, low-DOF samples). **Per-factor excess vs raw audit table** documented in `factor-codes.ts` header. **Annualisation audit** documented in `decomposition.ts` header (Σ ×252, idio daily ×252, α ×252, σ √252, R² none). **Beta-sensitivity sweep script** at `scripts/factor-sensitivity.ts` (`npx tsx scripts/factor-sensitivity.ts SPY,QQQ 30,60,90,252`). **3 new multicollinearity tests**, 103 / 103 vitest pass, `npx tsc --noEmit` clean.

- **2026-04-25 (factor tab MACRO14 overhaul):** Restructured `/factors` to a **Portfolio | Per-Stock** primary toggle. Replaced the academic-only model defaults with a new **MACRO14** preset (now the default) — 7 macro asset-class factors (`EQ`, `LOCAL_EQ`, `RATES`, `COMM`, `EM`, `FX`, `INFL`) and 7 style risk premia (`SHORT_VOL`, `TREND`, `BAB`, `MOM`, `QMJ`, `HML`, `CROWD`); CAPM/FF3/FF5/Carhart4/Extended remain available in the model dropdown. Window presets are now in trading days mapped to the user-facing 30 / 60 / 90 / 180 day, 365 day, 5 year + custom range; UI flags low-DOF combinations. **Per-Stock view** renders a Bloomberg-style heatmap grid (rows = every active universe constituent, columns = the 14 factors) with a `Beta | Return | Risk` metric toggle, sector / sub-theme filters, and a sticky right-side detail panel showing the selected stock's full β / return-contribution / risk-contribution × all factors plus diagnostics (R², total vol, systematic share, alpha). New backing service `src/server/services/factor-per-stock.service.ts` runs the same multivariate OLS engine per stock, cost-weighted is irrelevant here. **Coverage check** at `src/lib/factors/regression/coverage.ts` drops factors whose inception is after the requested window start (relevant for short-history series like DBMF (2019) and GVIP (2016)) and badges them in the UI. **Data pipeline expansion:** new `factor-pipeline-macro.service.ts` ingests 11 new tickers from Yahoo (`ACWI`, `IEF`, `DBC`, `EEM`, `UUP`, `TIP`, `SPY`, `USMV`, `QUAL`, `DBMF`, `GVIP`) plus index `^PUT`, all behind the existing worker pool / backoff. **AQR daily** Betting-Against-Beta and Quality-Minus-Junk series are pulled from AQR's free public XLSX downloads via a custom JSZip + regex parser (the SheetJS `xlsx` package OOMs on the 30MB files because the inflated worksheet XML exceeds Node's ArrayBuffer cap); `USMV-SPY` and `QUAL-SPY` proxies splice the recent ~2-month publish gap using the existing `normalizeProxyToFf` machinery. Schema: `FactorCode` enum extended with the 14 new codes (no migration drama). New API route `GET /api/analysis/factors/per-stock?model=&window=&sector=&subTheme=`. New deps: `xlsx` (kept for fixture-style use; primary parsing path uses JSZip directly). Total tests: **100 pass** (added 9 new for AQR parser + factor coverage). `npx next build` clean.

- **2026-04-20 (dev memory / OneDrive):** `npm run dev` now caps the Node heap at 4GB (`cross-env`); optional `npm run dev:turbo` uses Turbopack; `next.config.ts` sets `onDemandEntries` to evict idle compiled routes sooner in dev; Performance page pauses the 30s market-map refetch when the browser tab is hidden. **Operational:** avoid multiple concurrent `next dev` instances on the same repo (they corrupt `.next`); OneDrive sync frequently contends with `.next` webpack pack files (`EPERM`/`EBUSY`) — prefer a non-synced working copy for heavy development.

- **2026-04-20 (Bloomberg visual fidelity):** Analysis shell pushed closer to a **Bloomberg Terminal–inspired** aesthetic (not affiliated): maroon top chrome (`--bb-chrome`), amber ticker chips / row labels, flat panels (no radius), **Roboto Condensed** + **IBM Plex Mono**, denser `11.5px` typography, saturated signed heat ramps (`heatmap.ts` + shared `bbTooltipStyle` for Recharts), two-strip `TopBar` with horizontal **module tabs** only (duplicate left sidebar removed), `BloombergTabStrip` on Factors, and primitive/table/badge updates (`DataTable`, `MetricCard`, `StatusBadge`, `Toast`, `Skeleton`). `analysis.css` no longer pulls in `@tailwind base` (Preflight was resetting global body styles and could present as an unstyled white page).

- **2026-04-19 (factor refactor):** Rebuilt `/factors` as a consolidated institutional Factor Analysis tab. Replaced the legacy style z-score + univariate OLS approach with a joint multivariate OLS engine (`src/lib/factors/`) supporting CAPM / FF3 / Carhart4 / FF5 / EXTENDED (all-6) model presets with configurable rolling windows (20/60/120/252D) and optional exponential weighting. Key additions: `src/types/factors.ts` (shared type contracts); `src/lib/factors/` (engine: regression, attribution, risk, drivers, scenarios, market, formatting); `src/server/services/factor-engine.service.ts` (single orchestrator used by all factor API routes); new API routes under `/api/analysis/factors/{exposure, exposure/history, attribution, risk, drivers, scenarios/run, market, alerts}`; `factor-snapshot.service` (persists `FactorExposureSnapshot` on every compute, enabling history + drift alerts); `factor-alerts.service` (rules-based alerts: drift, concentration, alpha deterioration); `factor-drivers.service` (holdings-implied loadings by position/sector/sub-theme); `factor-scenarios.service` (synthetic + historical scenario shocks); `factor-market.service` (factor return heatmap + correlation matrix). UI rebuilt as a 7-section tab (Exposure / Attribution / Risk / Drivers / Scenarios / Market Context / Alerts) with `ControlsBar`, `HeaderSummary`, `ExposurePanel`, `TimeSeriesPanel`, `DriversPanel`, `ScenariosPanel`, `RiskPanel`, `MarketContextPanel`, `AlertsPanel`. Factor store settings persisted in Zustand. Attribution service rewritten to use joint regression (correct biases from correlated factors). Sidebar: Attribution removed, redirects to /factors. 33 new tests, all 91 tests pass.

- **2026-04-19:** Added full 9-module **Portfolio Analysis Suite** alongside the existing Market Map. New route group `(analysis)` with sidebar shell, dark design system (Inter + JetBrains Mono initially; CSS variable tokens in `analysis.css`). All 9 modules live under `/overview`, `/performance`, `/risk`, `/factors`, `/attribution`, `/stress`, `/concentration`, `/data`, `/alerts`. Prisma schema extended with `PortfolioPosition` (lot-based), `SecurityFundamentals`, `FactorReturnDaily`, `FactorPipelineStatus`, `RiskFreeRate`, `AppSetting`, `AuditLog`, `Alert`, `StressScenarioRun`. Providers added: `KenFrenchProvider` (ZIP→CSV), `FredProvider` (TB3MS), `YahooFundamentals`. Domain calcs: `var.ts` (parametric/historical VaR, CVaR, marginal, component, stressed), `drawdown.ts` → moved into `risk-adjusted.ts` (max DD, current DD, duration, Sortino, Calmar, up/down capture), `beta.ts` (OLS, Vasicek, Jensen's alpha, tracking error, rolling correlation), `correlation.ts`, `vol-decomp.ts` (systematic/idiosyncratic via R²), `distribution.ts` (skewness, kurtosis, histogram, monthly calendar, rolling 12m), `concentration.ts` (HHI, effective N, top-K, hierarchical clustering), `factor-pipeline.ts` (gap detection, 63d distribution-matching normalization, splice), `factor-scoring.ts` (winsorize, z-score, composite scores), `attribution.ts` (rolling 63d OLS attribution, Brinson allocation/selection, trade stats). Services: `position.service`, `pnl.service`, `performance.service`, `risk.service`, `concentration.service`, `factor-pipeline.service`, `factor.service`, `attribution.service`, `stress.service`, `alerts.service`, `data-refresh.service`. 25 new `/api/analysis/*` route handlers. UI: TanStack Query, Zustand, Recharts, custom Heatmap/Gauge/Donut/Sparkline/DataTable/MetricCard/InfoTooltip/ChartCard primitives. Build passes clean; 58 tests pass.

- **2026-04-18 (eve, +5):** Fixed silent Apply failures on large pastes (~700+ tickers). `replaceUniverseConstituents` was running 1 `upsert` + 1 `create` **per row** inside a single Prisma `$transaction`, blowing the default 5 s interactive-transaction timeout long before the 743-row paste finished. Rewrote it to (a) de-dupe input by uppercased ticker (last-wins) so duplicate pastes never violate `@@unique([universeId, securityId])`, (b) pre-load existing securities outside the transaction with one `findMany`, (c) inside the transaction issue a small fixed set of batched writes — `deleteMany` old rows → `createMany` new securities (`skipDuplicates`) → bulk `updateMany` to reactivate inactive tickers → per-row `update` only for the (typically tiny) set whose name actually changed → `findMany` to resolve IDs → single `createMany` for all `UniverseConstituent` rows — with an explicit `{ timeout: 30_000, maxWait: 10_000 }`. Returns a structured `{ applied, created, reactivated, renamed, duplicatesDropped }` summary. The `/api/universes/:id/constituents` POST route now sets `maxDuration = 120`, surfaces a JSON `{ ok: false, error }` body on failure (instead of crashing with an HTML page), and forwards the new counts. Manage Tickers modal upgrades: parse errors now show `"N row(s) failed — line X: msg · …and Y more"` (was: silently capped at 3 with no total), Apply errors are read as JSON and shown verbatim, the footer message colours red on failure / green on success, and the success line includes "X new · Y duplicate dropped" when relevant.
- **2026-04-18 (eve, +4):** Tightened the per-level visual rhythm on the Market Map so sectors, sub-themes, and tickers can be told apart at a glance. Each level now has its own **density** (sector rows breathe at ~0.7rem padding, sub-themes tighten to ~0.42rem, tickers compact to ~0.32rem), its own **typography** (sector = uppercase amber `#f0b65d` 700-weight with wider letter-spacing; sub-theme = cool light `#d8e3f0` 500-weight; ticker = monospace `#9eb0c8` 500-weight with letter-spacing), and its own **background tone** (`#1c2638` / `#0e1624` / `#091018`). Heat-cell font size + weight follow the same 3-step scale (0.86 → 0.84 → 0.8 rem) so each row visibly belongs to a level. The inter-sector gap row was deepened to `#06090f` and bumped to 12px so each sector card sits in a clearly visible groove. Spines + chevrons re-tuned to match (sector chevron amber, sub-theme chevron cool slate).
- **2026-04-18 (eve, +3):** Market Map redesigned around a **block-grouped hierarchy** that visually mirrors the way the user thinks about the data: each sector renders as a prominent uppercase amber header row with its own elevated background; expanding the sector reveals its sub-themes as a contiguous, slightly-recessed block sitting directly underneath, joined to the parent via a continuous "spine" (`box-shadow: inset 4px 0 0 …`) running down the sector column. Expanding a sub-theme nests ticker rows the same way, with a secondary spine through the sub-theme column. Internal seams within a block are suppressed (`borderBottom: transparent`) so each sector reads as one connected card, and a small `#0b1018` spacer row is rendered between adjacent sectors so the blocks don't visually bleed together. Sector / sub-theme / ticker rows now use distinct background tones (`#1a2333` / `#10182a` / `#0d1422`) instead of relying on row dividers. The single global control was renamed to **Show sub-themes / Hide sub-themes** (toggles only sector-level expansion, leaving ticker drill-down on the per-sub-theme chevron) so the workflow matches "compare sectors → break out into sub-themes → click a sub-theme to see tickers".
- **2026-04-18 (eve, +2):** Market Map UI replaced the flat row-level filter (Sector / Sub-Theme / Company) with a **hierarchical expandable tree**. Columns reordered to **Sector | Sub-Theme | Ticker | horizons**, with sectors as the default top-level rows. Each sector row is clickable to reveal its sub-themes; each sub-theme row is clickable to reveal its tickers. A new global expand/collapse toggle was added in the controls strip. The component now performs a single `rowLevel=COMPANY` fetch and computes sector / sub-theme aggregates client-side (average of leaf metrics per horizon, matching the server logic), and applies **per-level heatmap ranges** so sector / sub-theme aggregates retain visible contrast despite their tighter spread. URL-based drill (`?sector=…&subTheme=…&rowLevel=…`) is no longer used by the UI.
- **2026-04-18 (eve, +1):** Manage Tickers → **Current** tab now supports per-row removal. New `DELETE /api/universes/:id/constituents?ticker=…` endpoint backed by `removeUniverseConstituent` (deletes the `UniverseConstituent` row only; `Security` row is preserved because it can still be referenced by portfolios, factor snapshots, or price history). UI optimistically updates the visible list, decrements the constituent count, and notifies the dashboard via `onApplied` so the market map refetches.
- **2026-04-18 (eve):** Hardened Yahoo ingestion against rate-limit storms that were leaving most tickers with no prices on a fresh universe load. Chart endpoint now retries with exponential backoff on HTTP 401/429/5xx (Yahoo's throttle codes), the universe ingest runs a small fixed worker pool (concurrency 3, ~150 ms inter-request delay) instead of an unbounded sequential blast, and the route handler's `maxDuration` is raised to 600 s so a 400-ticker initial run isn't cut off mid-flight. Symbol normalisation is now precise: only single-trailing-letter dots become dashes (`BRK.B` → `BRK-B`), foreign exchange suffixes stay intact (`MC.PA`, `NOVO-B.CO`), and bare US index codes are auto-prefixed with `^` (`VIX` → `^VIX`, `DXY` → `^DXY`, `VVIX` → `^VVIX`).
- **2026-04-18 (pm):** Consolidated universe management into a **Manage Tickers modal** launched from the Market Map top bar (tabs: CSV Import with drag-drop, Paste Tickers, Current with per-column search). Removed `/universe` page (redirects to `/market-map`), collapsed app to a single default universe via `/api/universe/default`, extended `/api/parse-universe` to accept CSV/TSV with the same **4-column contract** as the paste path (Ticker, Name, Sector, Sub-Theme; headers optional), switched top-level shell to a dark theme matching the Performance dashboard. **Dashboard now self-updates**: on first load (and after Apply in the modal) the page fires a background `?onlyMissing=true` ingest for new tickers + benchmarks, the chart re-fetches every 30 s without a click, and the manual `↻` is reduced to a force-full-refresh affordance. Universe ingest is now per-ticker fault tolerant (one Yahoo failure no longer aborts the batch — failures returned in `failed[]` and surfaced as a warning); Yahoo symbol form is normalised (e.g. `BRK.B` → `BRK-B`).
- **2026-04-18:** Universe + market map + portfolio UIs; REST APIs (`/api/universes/*`, `/api/parse-universe`, `/api/benchmarks/ingest`, `/api/portfolios/*`); Yahoo chart/quote HTTP provider; `computeMarketMap` / portfolio analytics services; Vitest alignment tests; removed `yahoo-finance2` (Next bundle conflict).
- **2026-04-17:** App shell, `/api/health` (Prisma), domain calcs, feature stubs, initial tests.
- **2026-04-17 (init):** `docs/ARCHITECTURE.md`, Prisma schema, provider ports.
